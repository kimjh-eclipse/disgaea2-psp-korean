# 마계전기 디스가이아 2 PORTABLE (PSP) 한국어화 — 기술 핸드오프

작성일 2026-08-22 / 작업폴더 `C:\Emul\Switch\패치유틸.xdeltaUI\D2DHD_kr`

## ★ 베이스는 일본판이다
- **작업 대상: `Makai Senki Disgaea 2 Portable (Japan) (PSP) (PSN).iso`** (DISC_ID `ULJS00183`)
- 미국판 `Disgaea 2 - Dark Hero Days (USA).iso` (DISC_ID `ULUS10461`)은 **폐기**.
  자간 처리가 EBOOT 폭 테이블에 묶여 있고 그 EBOOT이 KIRK 암호화라 막힌다(§6, §7).
  현재는 **공식 영어 번역 대조 자료**로만 쓴다.
- JP는 2바이트 문자 자간이 고정이라 **EBOOT을 건드릴 필요가 전혀 없다**(§6-B).

**현재 도달점**
1. 한글 2,286자 + 가나 162자 공존 배치 — 인게임 검증 (§11-B, **§14 개편판이 최신**)
2. 문자열 길이 제약 없음 확인 — 원문보다 길게 써도 안전 (§11-C)
3. **`script00.dat` 1,093건 전량 번역 완료 — 인게임 검증** (§12)
4. **SCRIPTPACK 대사 주입 경로 완성 — 인게임 검증** (§13)
5. 미번역 일본어는 가나로 읽히고, 미번역 한자는 공백 (§14)

※ §11-B 의 "한글 2350자 / 코드 0x8840~" 는 **구버전**이다. 최신은 §14.

※ §1~§10 의 주소·오프셋 표기는 **US판 분석 기록**이다. 포맷 자체는 JP와 동일하지만
   LBA·런타임 주소·문자열 오프셋은 JP 값을 써야 한다(§6-B, §11-D 참조).

---

## 1. ISO 레이아웃 (한국어화 관련 파일만)

| 파일 | LBA | 크기 | 비고 |
|---|---|---|---|
| `PSP_GAME/SYSDIR/EBOOT.BIN` | 32 | 1,691,904 | KIRK 암호화(§7) |
| `PSP_GAME/USRDIR/START_US.LZS` | 166,400 | 4,995,259 | **텍스트·폰트 전부 여기** |
| `PSP_GAME/USRDIR/SCRIPTPACK.DAT` | 164,704 | 3,451,837 | NISPACK, talk/script 다수 |
| `PSP_GAME/USRDIR/TXPPACK.DAT` | 171,712 | 13,673,168 | NISPACK, 이미지 221개 |
| `PSP_GAME/USRDIR/DIS2_PSP_DATA` | 55,088 | 104,857,600 | **전체가 0x00 패딩** (내용 없음) |

`START_US.LZS` 다음 파일이 LBA 168,848 → **가용 슬롯 = (168848-166400)×2048 = 5,013,504 B.**
재압축 결과가 이 크기 이하면 제자리 교체 가능(§6).

도구: `tools/isodump.py <iso>` — ISO9660 트리 덤프(LBA·크기 출력).

---

## 2. NIS LZS 코덱 (`tools/nislzs.py`)

`START_*.LZS` 압축 형식. **압축·해제 양방향 바이트 단위 일치 검증 완료**
(`START_VM_US.LZS` → `START_VM2_US.DAT`, `START_VM2_US.LZS` → `START_VM_US.DAT` 완전 일치).

### 헤더 (16바이트)
```
+0x00  char[4]  "dat\0"
+0x04  u32      압축 해제 후 크기
+0x08  u32      압축 크기
+0x0C  u8       flag 바이트 (파일마다 다름: START_US=0xD5, START_VM2_US=0xD7)
+0x0D  u8[3]    패딩
+0x10           압축 데이터 시작
```

### 토큰
- `b != flag` → 리터럴 1바이트
- `b == flag`, 다음 바이트 `x == flag` → 리터럴 `flag` 1바이트
- `b == flag`, `x != flag` → 3바이트 토큰 `[flag][x][cnt]`
  - `disp = (x > flag) ? x - 1 : x` — **flag 값을 건너뛰므로 1 감산**
  - 출력 끝에서 `disp` 바이트 뒤로 가서 `cnt` 바이트 복사

### ★ 인코더 필수 제약: `cnt <= disp`
게임 내장 디컴프레서는 **겹침 복사(overlapping copy)를 지원하지 않는다.**
`cnt > disp`인 토큰을 만들면 파일 크기는 맞게 나오지만 그 지점 이후 데이터가 전부 어긋난다.

이걸 어겼을 때의 증상(실제로 겪은 것): 문자열 블록의 `\0\0` 구분자가 `\0`로 뭉개져
문자열 워크가 밀리고, 타이틀 메뉴에 엉뚱한 문자열("Everybody", "Accepting charge for")이 표시된다.
파일 단독 검증(자체 라운드트립)은 통과하므로 **RAM 대조로만 잡을 수 있다** (§8 검증법).

인코더는 `MAXD=0xFE`, 최소 매치 4바이트, 3바이트 해시체인 사용. START 전체 압축 약 23초.

---

## 3. START 아카이브 포맷

`START_US.LZS`를 풀면 9,778,160 B (0x9533F0) 단일 아카이브.

```
+0x00  u32       엔트리 수 (US=21)
+0x04  u8[12]    예약
+0x10  entry[n]  엔트리 배열, 각 0x20 바이트
         +0x00  u32     데이터 오프셋 (상대값)
         +0x04  char[28] 파일명 (NUL 종단)
```

**실제 데이터 위치 = 엔트리오프셋 + 헤더크기.**
헤더크기 = `0x10 + n*0x20` (US n=21 → **0x2B0**). 이 `+0x2B0` 보정이 핵심 — 빼먹으면
모든 멤버가 0x2B0 앞으로 밀려 파싱된다.

데이터 영역은 0x10 정렬. 파일 순서는 테이블 순서와 다를 수 있어 오프셋 정렬 후 처리해야 한다.
`0x2B0 ~ 0x369920` 구간(3.5 MB)은 테이블에 잡히지 않는 선행 블롭 — **그대로 보존**한다.

### US 멤버 21개

| 파일 | 절대오프셋 | 크기 | 용도 |
|---|---|---|---|
| effect0.psp3d | 0x369920 | 0x8410 | |
| waku.txp | 0x371D30 | 0x252970 | |
| anm0000.dat | 0x5C46A0 | 0x295310 | |
| **script00.dat** | 0x8599B0 | 0x4780 | **UI 문자열 블록 1130개** |
| **InProgramTxtDB.dat** | 0x85E130 | 0x3E6B0 | NUL 구분 UI 문자열 다수 |
| **talk00.dat** | 0x89C7E0 | 0x5AC0 | **문자코드→글리프ID 맵** |
| **fontB.fnt** | 0x8A22A0 | 0x5AC0 | **글리프 폭/오프셋 테이블** |
| **fontB.ftd** | 0x8A7D60 | 0x8050 | **글리프 텍스처 512×128** |
| **FontB0000.txp** | 0x8AFDB0 | 0x4110 | 글리프 페이지 256×128 |
| sys2.txp | 0x8B3EC0 | 0x1DEA0 | |
| char.dat / magic.dat / charhelp.dat / dungeon.dat / HABIT.dat / mitem.dat / music.dat / WISH2.dat / comb.dat / face.dat | 0x8D1D60~ | | 각종 데이터 테이블 |
| mp50001.datpsp | 0x9533F0 | 0 | 빈 엔트리 |

재빌드: `tools/rebuild.py` → `rebuild_start(orig_bin, overrides_dict)`

---

## 4. TXP 텍스처 포맷 (`tools/txp.py`)

4bpp 인덱스 컬러 + PSP 스위즐. **인코드/디코드 라운드트립 바이트 일치 검증 완료.**

```
+0x00  u16  width
+0x02  u16  height
+0x04  u16  (0x10)
+0x06  u16
+0x08  u16  (0x10)
+0x0A  u16  pb   ← 팔레트 블록 수, 색 수 = 16 * pb
+0x0C  u16
+0x0E  u16
+0x10       팔레트: RGBA 4바이트 × (16*pb)
그 뒤        4bpp 픽셀, width*height/2 바이트, 스위즐 적용
```

픽셀 바이트 = 저니블 먼저(짝수 x), 고니블 나중(홀수 x).

### 스위즐 (PSP 표준 16×8 블록)
`rowbytes = width/2`. 블록 순서: 위→아래 블록행, 각 블록행 안에서 좌→우 블록,
각 블록은 8줄 × 16바이트 연속.

```python
def unswizzle(data, rowbytes, h):
    out = bytearray(len(data)); i = 0
    for by in range(h//8):
        for bx in range(rowbytes//16):
            for y in range(8):
                dst = (by*8+y)*rowbytes + bx*16
                out[dst:dst+16] = data[i:i+16]; i += 16
    return bytes(out)
```

### 크기 검산 (전부 일치 확인)
| 파일 | w×h | pb | 팔레트 | 픽셀 | 총합 |
|---|---|---|---|---|---|
| US fontB.ftd | 512×128 | 1 | 0x40 | 0x8000 | 0x8050 ✓ |
| US FontB0000.txp | 256×128 | 4 | 0x100 | 0x4000 | 0x4110 ✓ |
| JP FontB0000.txp | 512×512 | 16 | 0x400 | 0x20000 | 0x20410 ✓ |
| JP FontB0001.txp | 256×128 | 16 | 0x400 | 0x4000 | 0x4410 ✓ |

팔레트 알파는 0,17,34,…,255 균등 램프(16단계). 글리프는 알파 채널로 표현.

### JP vs US 폰트 규모
- **JP**: `FontB0000.txp` 512×512 + `FontB0001.txp` 256×128 — SJIS 수천 자 수록
- **US**: `fontB.ftd` 512×128 + `FontB0000.txp` 256×128 — 실사용 글리프 364개 수준

→ 한글 대량 수록은 **JP식 512×512로 확장**하는 경로가 실증되어 있다.
코드가 참조하는 페이지 파일명은 `FontB0000`/`FontB0001` **2개로 하드코딩**
(US 문자열 테이블 0x8982B34~, JP 0x8975510~).

글리프 셀은 14×14, 512폭 기준 한 줄 36칸(`COLS=36`).

---

## 5. 문자코드 → 글리프 (EBOOT 함수 역해석 완료)

변환 함수: **US `0x088A80D8` / JP `0x088A8000`** (동일 로직, 주소만 다름)

```
ASCII (0x20 ≤ c < 0x7F):
    idx = c - 0x20

2바이트 (SJIS 스타일, c1=선행, c2=후행):
    row = (c1 <= 0x9F) ? (c1 - 0x81) : (c1 - 0xC1)
    idx = 0x5F + row * 192 + (c2 - 0x40)
```

MIPS 원문 대응(US 0x88A8E04~):
```
sll  v1, a0, 1      # row*2
addu v1, v1, a0     # row*3
sll  v1, v1, 6      # row*192
addiu v1, v1, 0x5F  # +0x5F
addu v1, a1, v1     # + (c2-0x40)
```

### 맵/메트릭 테이블 (`talk00.dat`, `fontB.fnt` — 동일 레이아웃)
```
+0x00  u16  엔트리 수 (11615 = 0x2D5F)
+0x02  u16[]  인덱스별 값 → 접근: offset = 2 + 2*idx
```
- `talk00.dat` : `glyphID = u16[idx]`, 0이면 미매핑(글리프 없음)
- `fontB.fnt`  : `(width << 8) | xoffset` — **렌더링용 글리프 폭/오프셋** (advance 아님, §6 주의)

테이블 용량 11,615칸 중 US 실사용 176칸 → **여유 칸이 압도적으로 많다.** 한글 매핑 공간 충분.

### 기타 규명된 EBOOT 함수 (US)
| 주소 | 역할 |
|---|---|
| `0x088A80D8` | 문자코드 → 글리프ID 변환 |
| `0x088A993C` | 문자열 그리기 메인 루프 |
| `0x08916890` | `GetString(id)` — 로드시 구축한 포인터 배열 참조 |
| `0x0891694C` | **문자 advance(자간) 계산** ← §6 |

---

## 6. ★ 자간(advance) — 최대 함정

**2바이트 문자의 advance는 `fontB.fnt`가 아니라 EBOOT 함수 `0x0891694C`가 결정한다.**
fnt의 width를 고쳐도 자간은 바뀌지 않는다(실측 확인).

### 폭 함수 구조
원본 테이블 `0x089828DC`에서 0x1F0바이트를 스택으로 복사한 뒤 코드 구간별로 인덱싱:

```
width = table[code - 0x8140] + 2
```

커버 구간(SJIS 기호·가나 영역만):
`0x8140–0x81AB`, `0x81C0–0x81CF`, `0x81F2–0x81F4`, `0x824F–0x829A`,
`0x8390–0x83DF`, `0x8440–0x84A8`, `0x8740–0x876A`, `0x8793`

**위 구간에 없는 코드는 전부 기본 폭 5로 떨어진다.**

```
0x08916B14 :  li v0, 5     (= 0x24020005)   ← 기본 폭
```

한글에 쓰는 코드(0x8840~)는 커버 구간 밖 → 폭 5 → **글자가 서로 겹쳐 렌더된다.**

### 해결 (검증 완료)
```
0x08916B14 : 0x24020005  →  0x2402000E   (li v0, 14)
```
**단 1개 명령**으로 해결. RAM 라이브 패치로 적용해 "시작/이어하기/설정" 자간 정상 렌더 확인.

CWCheat 형식: `_L 0x28916B14 0x2402000E` (Disc ID `ULUS-10461`)

---

## 6-B. ★ JP판 자간 — 글리프 폭 자체는 패치 불필요 (검증 완료)

> **보강(§32)**: 아래 결론은 **글자** 자간에는 그대로 유효하다. 다만 **띄어쓰기는
> 예외**로, 전각 공백이 한글과 같은 15px 을 먹어 §32 에서 7px 로 패치했다.
> ASCII 공백은 `fontB.fnt` 을 아예 보지 않고 부동소수 `f22` 에서 자간을 가져온다.

**결론: JP판은 2바이트 문자에 고정 자간을 주므로 EBOOT을 건드릴 필요가 전혀 없다.**
US판의 "구간 테이블 + 기본폭 5 폴백" 구조(§6)가 JP엔 존재하지 않는다.

JP 그리기 함수 **`0x088A8748`** (루프 머리 `0x088A8BEC`). 프롤로그에서 자간 상수 3개를 미리 계산:

| 스택 슬롯 | 계산 | 스케일 1.0일 때 |
|---|---|---|
| `0x1C0(sp)` | `lui 0x40E0` = 7.0 × f12 | **7** |
| `0x1C4(sp)` | `lui 0x4160` = 14.0 × f12 | **14** |
| `0x1BC(sp)` | `lui 0x4170` = 15.0 × f12 | **15** |

문자별 선택 (`0x088A8C48`~`0x088A8C90`):
```
a0 = (0x41<=c<0x5B) || (0x61<=c<0x7B)      # 영문 대소문자 여부

if (c1==0x84 && c2==0x9F):   자간 = 0x1C4 = 14      # 특수문자 전용
elif a0 == 0:                자간 = 0x1BC = 15      # ★ 2바이트 전각 등 전부
else:                        자간 = 0x1C0 = 7       # 영문자
```
※ `0x088A8C7C`가 `beql`(likely)이므로 분기 성립 시에만 지연슬롯 `lw v0,0x1BC(sp)`가 실행된다.

일반 경로 전진: `0x088A9D1C  lw v1,0x1C8(sp)` → `0x088A9D20  addu s5,s5,v1`

**즉 문자코드값과 무관하게 2바이트 문자는 15px 고정 전진.**
한글을 어떤 2바이트 코드(0x8840대 등)에 넣어도 자간이 정상이다. 14×14 글리프 셀과 잘 맞는다.

### JP 런타임 주소 (검증됨)
| 대상 | 주소 |
|---|---|
| **START 아카이브 RAM 베이스** | `0x08DB7B80` ← 파일과 바이트 일치 검증 완료 |
| 문자맵 (talk00.dat) | `0x0963B240` |
| 메트릭 (fontB.fnt) | `0x09640D00` |
| 문자열 블록 (script00.dat) | `0x095F6080` |
| 그리기 함수 | `0x088A8748` |
| 글리프 블릿 | `0x088A7ED8` |

JP `script00.dat`: 문자열 **1105개** (US 1130개), 파일내 절대오프셋 `0x83E500` (= 엔트리값 `0x83E250` + `0x2B0`)

---

## 7. EBOOT 상태 (US판 한정 이슈)

```
~PSP 헤더 (ext/EBOOT.BIN)
+0x00  "~PSP"
+0x06  u16  comp_attr = 0x0002
+0x0A  name = "Disgaea"
+0x28  u32  0x0019CFA4
+0x2C  u32  elf_size = 0x0019D100
+0x30  u32  load_addr = 0x08804000
+0x34  u32  entry_off = 0x00166904
+0x38  u32  modinfo_off = 0x004920D0   ← elf_size 초과 = 헤더값이 평문 아님
```

**KIRK 암호화 상태.** 0x150 이후 구간 엔트로피 7.95(≒난수) — 단순 압축이 아니다.
gzip/KL4E/KL3E/2RLE/ELF 매직 없음. 따라서 **디스크상의 EBOOT을 직접 수정할 수 없다.**

런타임 모듈 정보(RAM에서 확인):
```
sceModuleInfo @ 0x0896A8B0 : name="Disgaea"
  libent_end   0x089A8A20
  libstub_top  0x0896A6C8
  libstub_end  0x0896A6D8
entry @ 0x0896A904
```

§6 폭 패치를 ISO에 영구 반영하는 방법은 미해결 상태 — 별도 과제(§10).

---

## 8. PPSSPP 동적분석 환경 (`tools/ppsspp_ws.py`, `tools/ppsspp_dbg.py`)

WebSocket 디버거. **포트 4543**, 경로 `/debugger`.
(`ppsspp.ini`의 `RemoteDebuggerOnStartup = True` 필요 — 이미 켜져 있음)

### 동작 확인된 이벤트
| 이벤트 | 용도 |
|---|---|
| `memory.read {address,size}` | base64로 반환 |
| `memory.write {address,base64}` | 라이브 패치 |
| `memory.breakpoint.add {address,size,read,write}` | 워치포인트 |
| `memory.breakpoint.remove` / `.list` | |
| `cpu.status` | stepping/pc/ticks |
| `cpu.resume` | 재개 |
| `cpu.getAllRegs` | `categories[].name=="GPR"` 안의 `registerNames`/`uintValues` |
| `memory.disasm {address,count}` | **매우 유용** — 심볼·데이터접근·분기타겟·문자열까지 해석해 줌 |
| `input.buttons.press {button}` | `start`/`cross`/`circle` 등 |

주의사항:
- 워치포인트 히트는 `cpu.stepping` 이벤트로 통보됨
- WebSocket 프레임이 **분할 전송**되므로 FIN 비트까지 모아야 JSON 파싱 가능
- `gpu.buffer.screenshot`은 stepping 상태에서도 실패 → **화면 캡처는 Win32 `CopyFromScreen`** 사용
- 프레임버퍼 `0x04000000` 직접 읽기는 타일링돼 있어 그대로는 그림이 안 나옴

### ★★ 함정: RAM 덤프는 JIT 마커로 오염된다
`memory.read`로 뜬 코드 영역 덤프에는 PPSSPP의 **JIT emuhack 옵코드(`0x68xxxxxx`, opcode 0x1A)**가
섞여 있다. 실측: US 1.0%, JP 2.0%의 코드 워드가 이것으로 덮여 있음.

**이 마커는 함수 프롤로그와 분기 타겟에 정확히 얹힌다.** 따라서 덤프를 대상으로
프롤로그·jal·분기 패턴을 검색하면 **거짓 음성(없다고 결론)** 이 난다.

실제로 이 함정 때문에 "JP엔 폭 함수가 없다"는 오진을 한 번 냈다. (JP도 가변 자간을 쓴다 —
`0x88A9D20`의 `addu s5,s5,v1`. v1을 세팅하는 `0x88A9D1C`가 JIT 마커로 덮여 정적 판독 불가)

**대응**:
- 코드 판독은 **`memory.disasm` API를 쓴다** — PPSSPP가 emuhack을 풀어 올바른 명령을 돌려준다.
- 또는 JIT를 끄고(인터프리터) 덤프한다. ※ 설정 변경은 사용자 허락 필요.
- 덤프 기반 패턴 검색 결과가 "0곳"이면 **결론 내리지 말고** API로 교차확인한다.
- 파일 기반 작업(LZS·TXP·아카이브)은 이 오염과 무관하다.

### 런타임 주요 주소 (US, 참고값)
| 대상 | 주소 |
|---|---|
| 폰트 전역 블록 | `0x08C795C0` (divisor 324, fontinfo 포인터 3개) |
| 로드된 문자맵 (talk00) | `0x097473A0` |
| 로드된 메트릭 (fontB.fnt) | `0x0974CE60` |
| 문자열 포인터 배열 | `0x08E190F4` (개수 1130 @ `0x08E190F0`) |
| START 아카이브 RAM 베이스 | `0x08EAABC0` |
| 폭 테이블 원본 | `0x089828DC` |

※ 이 주소들은 부팅 세션마다 동일했으나, 확인 후 사용 권장.

### ★ 패치 무결성 검증법 (강력 추천)
게임이 푼 RAM 이미지와 내가 만든 아카이브를 **바이트 단위로 대조**한다.

```
START RAM 베이스 = 0x08EAABC0
for off in range(0, len(my_archive), 0x100000):
    chunk = dbg.read(BASE+off, ...)
    assert chunk == my_archive[off:off+len(chunk)]
```

- **클린 ISO로 이 대조를 하면 diff 0** — 즉 대조 기준선이 완벽하다.
- LZS 인코더가 §2 제약을 위반하면 여기서 수만~수십만 diff로 즉시 드러난다.
- 파일 단독 라운드트립만으로는 이 버그를 절대 못 잡는다. **반드시 이 대조를 거칠 것.**

---

## 9. 문자열 패치 실전 (`script00.dat`)

### 블록 포맷
```
+0x00  u32  count (1130)
+0x04  u32  count (중복)
+0x08       문자열 시작
```
각 문자열은 NUL 종단 + **추가 NUL 1개**(즉 `\0\0` 구분). 워크 방식:
```python
a = 8
for i in range(count):
    ptr[i] = a
    while data[a] != 0: a += 1
    a += 2          # NUL + 구분 NUL
```
빈 문자열도 정상 처리된다(`data[a]==0`이면 바로 +2).

### 검증된 패치 지점 — 타이틀 메뉴
| 대상 | 파일 오프셋 | 원문 |
|---|---|---|
| 타이틀 메뉴 클러스터 | **0x395A** | `Start\0\0Continue\0\0Save the file?` |
| 단독 Setting | **0x08A6** | `\0Setting\0` |
| (다른 화면용) | 0x392D | `Start\0\0Continue\0\0Setting\0` ← **타이틀 메뉴 아님** |

주의: 0x392D 클러스터만 고치면 타이틀 메뉴는 영어로 그대로 남는다(실측).
같은 문자열이 여러 벌 존재하므로 **어느 인스턴스가 쓰이는지 런타임 문자열 테이블로 확인**할 것.
확인법 — `0x08E190F4 + id*4`의 포인터를 읽어 실제 표시 문자열을 역추적.

### 길이 제약
문자열 블록은 워크 기반이라 **길이 변경 시 이후 모든 ID가 밀린다.**
안전하게 가려면 **원문과 정확히 동일한 바이트 길이**를 유지하고 남는 칸은 공백으로 채운다.
(길이를 늘리려면 블록 전체를 재구성하고 count/구분자 규칙을 지켜야 함 — 미시도)

### PoC 매핑 (현재 빌드에 반영된 상태)
| 문자 | 코드 | 글리프ID |
|---|---|---|
| 시 | 0x8840 | 206 |
| 작 | 0x8841 | 207 |
| 이 | 0x8842 | 200 |
| 어 | 0x8843 | 201 |
| 하 | 0x8844 | 202 |
| 기 | 0x8845 | 203 |
| 설 | 0x8846 | 204 |
| 정 | 0x8847 | 205 |

베이크 폰트: `C:\Windows\Fonts\malgunbd.ttf` 13pt, 14×14 셀 중앙 정렬, 알파 4비트(값>>4).

---

## 10. 빌드 파이프라인 요약

```
1) START_US.LZS 추출 → nislzs.decompress()          → out_start.bin
2) 멤버 추출 (오프셋 +0x2B0 보정)                     → ext/start3/
3) 수정: fontB.ftd(글리프) / talk00.dat(맵) /
         fontB.fnt(폭) / script00.dat(문자열)         → build/
4) rebuild.rebuild_start(out_start.bin, overrides)
5) nislzs.compress(raw, 0xD5)  ※ cnt<=disp 제약 준수
6) decompress(c) == raw 자체검증
7) inject_iso()  — 데이터 기록 + ISO LBA 25 디렉터리
                   레코드의 크기 필드(LE @+10, BE @+14) 갱신
8) RAM 대조 검증 (§8) ← 생략 금지
9) 자간 패치 0x08916B14 → 0x2402000E 적용 후 인게임 확인
```

### 도구 목록
| 파일 | 용도 |
|---|---|
| `tools/isodump.py` | ISO9660 트리 덤프 |
| `tools/isoext.py` | ISO에서 파일 추출 |
| `tools/nislzs.py` | LZS 압축/해제 (양방향 검증됨) |
| `tools/txp.py` | TXP 인코드/디코드 (라운드트립 검증됨) |
| `tools/rebuild.py` | START 재빌드 + ISO 주입 |
| `tools/ppsspp_ws.py` | WebSocket 최소 클라이언트 |
| `tools/ppsspp_dbg.py` | 디버거 래퍼 (read/write/bp/disasm) |

---

## 11. 작업 방침 — JP판 베이스로 확정

**베이스: `Makai Senki Disgaea 2 Portable (Japan) (PSP) (PSN).iso`** (US판 아님)

이유:
1. **EBOOT 문제 소멸** — JP는 2바이트 자간이 고정(§6-B). US판의 폭 테이블 폴백(§6)이 없어
   EBOOT을 손댈 필요가 전혀 없다. US판의 KIRK 암호화 벽(§7)이 무의미해진다.
2. **글리프 공간** — JP는 `FontB0000` 512×512 + `FontB0001` 256×128 (§4).
   14×14 셀 기준 페이지0에 36×36=1296칸. 기존 한자·가나를 전부 밀고 한글로 채우는 방침에 부합.
3. **번역 원문** — 일본어 원문 직역 가능. 동시에 US판 공식 영어 번역을 대조 자료로 보유.

### US판에서 그대로 재사용되는 것 (JP 파일로 검증 완료)
- NIS LZS 코덱 (§2) — `START_JP.LZS` 정상 해제, 동일 포맷 + 동일 `cnt<=disp` 제약
- START 아카이브 구조 (§3) — `+0x2B0` 보정 동일, JP 멤버 21개 추출 성공
- TXP 코덱 (§4) — JP 512×512 / 256×128 디코드 성공
- 문자코드→글리프 공식 (§5) — JP 함수 로직 US와 동일
- 맵/메트릭 테이블 포맷 (11,615칸) — 동일
- PPSSPP 도구체인 + RAM 대조 검증법 (§8)

### JP 기준으로 다시 잡아야 하는 것
- ISO 슬롯: `START_JP.LZS` LBA **249,824** / 크기 **5,012,594** (다음 파일 LBA 252,272 → 슬롯 5,013,504)
- 문자열 패치 오프셋 — JP `script00.dat`는 1105개, 크기 0x4A30 (US와 다름)
- 런타임 주소 — §6-B 표 참조

---

## 11-B. 한글 폰트 베이크 — 완료 (인게임 검증됨)

**결과: JP 타이틀 메뉴에 "처음부터 / 이어하기" 정상 표시. EBOOT 패치 없이 자간까지 정확.**

### 확정된 격자 (알려진 일본어 글자 역추적으로 검증)
`あ い ア 亜 一 学 魔` 7자를 코드→맵→셀 좌표로 잘라내 전부 정확히 일치 확인.

| 항목 | 값 |
|---|---|
| 셀 크기 | **14 × 14** |
| 페이지당 열 수 | **36** |
| divisor (페이지당 글리프) | **1296** (36×36) |
| page0 | `fontB.ftd` 512×512 → 1296칸 |
| page1 | `FontB0000.txp` 512×512 → 1296칸 |
| page2 | `FontB0001.txp` 256×128 → 162칸 |
| gid → 위치 | `page=gid/1296`, `cell=gid%1296`, `row=cell/36`, `col=cell%36` |

### 구간별 gid 배치 (JP 원본)
| 구간 | idx | gid | 처분 |
|---|---|---|---|
| ASCII 0x20-0x7E | 0-94 | 1-77 | **보존** |
| lead 0x81 (전각기호·버튼아이콘 ○△×□) | 95-286 | 78-131 | **보존** |
| lead 0x82-0x87 (가나 등) | 287-1438 | 132-374 | 폐기 |
| lead 0x88+ (한자) | 1439+ | 375-2489 | 폐기 |

### 한글 배정 (확정)
- 보존: gid 1..131 / idx 0..286
- **한글 2350자 (KS X 1001 완성형 전량) → gid 132..2481**
- 코드: `n`번째 한글 → `c1 = 0x88 + n/192`, `c2 = 0x40 + n%192` (0x8840 ~ 0x946D)
- `n = (euckr_c1-0xB0)*94 + (euckr_c2-0xA1)`
- `fontB.fnt` 한글 엔트리 = `0x0000` (한자와 동일 = 전각 풀셀)
- 폰트: **돋움 (`C:\Windows\Fonts\gulim.ttc` index 2) 12px**, 14×14 셀 중앙정렬, 알파 4bpp(`v>>4`)
- 용량: 페이지0+1 = 2592칸, 보존 131칸 제외 **2460칸 가용 > 2350자** (110칸 여유)

### ★ 부작용 (설계상 불가피)
한자·가나 글리프를 밀어버렸으므로 **번역 안 된 일본어는 엉뚱한 한글로 렌더된다.**
예: `設定`(SJIS 90DD 92E8) → n=1693/2088 → "짖틉". 즉 **전 텍스트 번역이 전제**다.
(타이틀 메뉴 3번째 항목이 이렇게 보여서 발견 → 원인은 별도 문자열 id 0x077 미패치였음)

---

## 11-C. 번역 분량 실측

`tools/inventory.py` 로 전 소스 통합 + 원문바이트 기준 중복 제거:

| 항목 | 수치 |
|---|---|
| 소스 | `script00.dat`, `InProgramTxtDB.dat`, SCRIPTPACK 34개(talk×17, script×17) |
| 총 등장 | 68,231회 |
| **고유 문자열** | **13,596건** |
| **고유 일본어 문자** | **200,017자** |
| 중복률 | talk 파일 간 **80.7%** (talk01↔talk02 교집합 80%) |
| 문자열 길이 | 중간값 32B, 평균 29B, 최대 56B |

→ talk 파일 17개는 공통 대사를 대량 공유. **한 번 번역해 전 occurrence에 적용**해야 하고,
같은 원문은 반드시 동일하게 번역해야 한다(일관성).

산출물: `work/inventory.tsv` (n, bytes, occ, jp, ko) — ko 열을 채우는 방식.

### ★ 길이 제약 없음 — 검증 완료
**문자열을 원문보다 길게 써도 된다.** 인게임 확인 완료.

근거: 문자열 블록은 로드시 워크로 포인터 배열을 만들므로(US 함수 `0x89168E8`),
**count만 유지하면** 개별 문자열 길이는 자유다. 포인터 배열 크기는 count로 결정된다.

검증 방법/결과: 타이틀 메뉴 3개를 원문의 2~3배로 늘려 빌드
- `設定`(4B) → `환경설정하기`(12B)
- `はじめから`(10B) → `맨처음부터 새로 시작`(22B)
- `つづきから`(10B) → `저장한 데이터 이어하기`(24B)

→ 3항목 전부 정상 렌더, 크래시 없음, **뒤 문자열 밀림 없음**(포인터 배열이 새로 계산되므로).
블록 총 크기 +64B 증가도 문제 없음.

**블록 재구성 규칙** (`tools/textio2.py`):
```
u32 count | u32 count | (문자열 + \0 + \0) × count | NUL 패딩 8B
```
- count는 절대 바꾸지 말 것 (포인터 배열 크기 결정)
- 말미 8바이트 NUL 정렬 패딩 유지 (원본 script00.dat은 0x4A28에서 끝나고 0x4A30까지 패딩)
- 무수정 재구성 시 원본과 바이트 일치함 — 회귀 검사로 쓸 것

**실질 제약은 두 가지뿐**:
1. START 압축본이 ISO 슬롯(5,013,504B) 안에 들어갈 것 — 현재 97.2%, 여유 142KB
2. 화면 폭 (자간 15px × 글자수가 대화창을 넘지 않을 것) — 게임 자체 개행 처리에 의존

---

## 11-D. 빌드 파이프라인 (JP)

```
python tools/build_jp.py --iso
```
1. `work/ko_script00.tsv` (id, ko) 읽어 script00 패치 (길이초과 시 예외)
2. `build_jp/` 폰트 4종 + script00 으로 START 재빌드
3. LZS 압축 + 자체검증 + **비중첩 제약 검사**
4. 슬롯 검사 후 `build_jp/D2_JP_KR.iso` 주입 (LBA 249824, 슬롯 5,013,504B)

현재: 4,871,209B / 5,013,504B (97.2%) — 여유 142KB

도구 추가분:
| 파일 | 용도 |
|---|---|
| `tools/krfont.py` | 한글 글리프 베이크 + 코드 매핑 |
| `tools/krtext.py` | 한글 → 게임 바이트 인코더 (`build_jp/hangul_codes.tsv` 사용) |
| `tools/textio.py` | 문자열 블록 덤프/패치 |
| `tools/inventory.py` | 전 소스 통합 인벤토리(중복제거) |
| `tools/build_jp.py` | 전체 빌드 |

---

### 남은 과제
1. **번역** — 고유 13,596건 / 20만 자. `work/inventory.tsv` ko 열 채우기.
   ※ 기계번역 금지, 직접 번역. 동일 원문은 동일 번역(일관성).
3. **InProgramTxtDB.dat 포맷 규명** — 헤더 u32 count(1988)×2 + 레코드(스트라이드 0x20 추정)
   + 문자열 풀 구조. script00 형식과 다름. 패치하려면 별도 파서 필요.
4. **SCRIPTPACK 재패킹** — NISPACK(엔트리 0x2C: 이름32+off+size+해시). talk/script 34개.
   재패킹 도구 미작성.
5. **최종 배포** — xdelta 생성. 실기 배포는 대상 아님(에뮬레이터 전용).

---

## 12. 작업 규칙 (에뮬레이터)

1. 실행·종료는 하되 **빠른 반복 실행 금지**. 한 번 띄우면 확인 항목을 한 번에 처리.
2. **설정 변경은 사전 허락 필수** (`ppsspp.ini` 포함).
3. **정상 종료** — `taskkill /F` 강제종료 금지.
4. 백그라운드 프로세스(에뮬레이터·디버거 스크립트) 방치 금지, 그 자리에서 정리.

---

## 12-B. 번역 진행 현황

| 소스 | 고유 문자열 | 상태 |
|---|---|---|
| `start/script00.dat` | 1,093 | **번역 완료 · 인게임 검증됨** |
| `start/InProgramTxtDB.dat` | (포맷 미규명) | 미착수 |
| SCRIPTPACK 34개 (talk×17, script×17) | 나머지 | 미착수 (재패킹 도구 필요) |
| **전체 고유** | **13,596** | script00 완료 |

### 번역 관리 체계
- **단일 원본 = `work/tr_batch*.py`** (dict `T = {id: '한글'}`)
  ※ 과거에 TSV를 손으로 고쳤다가 병합 스크립트가 덮어써 타이틀 메뉴가 날아간 적 있음.
    반드시 배치 파일에만 쓸 것.
- `python tools/merge_tr.py` → 병합 + 검증 → `work/ko_script00.tsv` 생성
  - 인코딩 불가 문자 검출 (글리프 없는 문자)
  - 서식지정자(`%`) 개수 불일치 검출
  - 배치 간 충돌 검출
- `python tools/build_jp.py --iso` → 빌드 + ISO 주입

### 번역 규칙
- 원문 길이 무시 가능(§11-C). 자연스러운 한국어 우선
- `%s` `%d` `%2d` `%02d` `%3d` 서식지정자 **개수·순서 유지**
- 보존 문자 그대로 사용: `○△□×` 버튼아이콘, `！？（）「」『』：・…→←〜−＋／％￥∞ ─ ―`
  전각공백 `　` 포함. 전각영숫자(ＡＢ０１)는 **ASCII로 변환**해 쓸 것(글리프 없음)
- 가나·한자는 절대 사용 불가(글리프 삭제됨) — `krtext.validate()`가 검출

### 확정 용어 (일관성 유지 필수)
베이스 패널 / 시공 게이트 / 초시공 게이트 / 이상한 게이트 / 기묘한 게이트 / 재판소 게이트 /
마계 재판소 / 다크 태양 / 마체인지 / 헬(HL) / 마나 / 아이템계(`界`=계) / 계적단 /
이노센트 / 재판꾼 / 휴대 주머니 / 창고 / 의회 / 의원 / 뇌물 / 표결 / 발언력 / 호감도 /
로젠퀸 상회 / RQ 상회 / 제니스키 / 웨폰 마스터리 / 리시브 / 칸(`マス`) / 스승·제자 /
소질(답 없는 쓰레기·낙오자·평범·우수·극히 우수·천재) / 전생 / 프리니 / 아쿠타레 /
로자린드 / 아델 / 에트나 / 라하르 / 플로네 / 제논 / 홀루트 마을 / 수라 · 나장

---

## 13. SCRIPTPACK (대사 본체) — 주입 경로 완성 · 인게임 검증

### NISPACK 포맷 (`tools/scriptpack.py`) — 재패킹 바이트 일치 검증 완료
```
+0x00  char[8] "NISPACK "
+0x08  u32     0
+0x0C  u32     count (34)
+0x10  entry[count], 각 0x2C
         +0x00 char[32] 파일명
         +0x20 u32 offset
         +0x24 u32 size
         +0x28 u32 해시/타임스탬프  ← 보존
데이터는 0x800 정렬. **마지막 파일 뒤에는 패딩 없음**(원본이 그러함)
```
멤버 34개 = `talk01~20_jp.dat` 17개 + `script01~19.dat` 17개

### talkXX_jp.dat 포맷 (`tools/talkfile.py`) — 왕복 바이트 일치 17/17
```
+0x00  u32 count
+0x04  u32 count (중복)
+0x08  record[count], 각 0x20
         +0x00 u32 stream_offset   ← ★ 레코드 테이블 끝 기준 바이트코드 오프셋
         +0x04.. 나머지 필드 보존
table_end = 8 + count*0x20
이후    바이트코드 스트림
```
**문자열은 `01 <SJIS...> 00` 패턴으로 인라인** — 16,278개 전수 조사에서 100% 이 패턴.
(문자열 직전 바이트가 전부 `0x01`, 직후가 전부 `0x00`)

검증: talk01 은 `table_end 0x7948 + 마지막 field0 0x29E9E = 0x317E6`, 파일 `0x317EE` (말미 8B).
talk05 도 동일 규칙 → **field0 은 오프셋이 맞다.**

### ★ 길이 변경 시 오프셋 보정 필수
문자열 길이를 바꾸면 이후 모든 `stream_offset` 이 밀린다.
`talkfile.rebuild()` 가 (old_pos → 누적delta) 맵으로 전 레코드를 자동 보정한다.
- 문자열을 절대오프셋으로 참조하는 곳은 없음(4,293개 중 우연 일치 25건뿐 → 참조 아님)
- **무수정 재구성이 원본과 바이트 일치** — 회귀 검사로 항상 먼저 돌릴 것

### script*.dat 17개는 다른 포맷
`talkfile.parse()` 가 거부한다. 일본어 텍스트가 거의 없음(script09 21건, script16 2건, 나머지 0).
필요해지면 별도 파서 작성.

### ★ ISO 슬롯 부족 → 끝에 재배치 (`tools/isopatch.py`)
`SCRIPTPACK.DAT` 원본 슬롯: LBA 248,176, 다음 파일 249,824 → **3,375,104B (여유 12,306B뿐)**
한글이 일본어보다 길어지는 경우가 많아(`・戻る` 6B → `・돌아가기` 12B) 전량 번역 시 확실히 초과한다.

`isopatch.replace()` 동작:
1. 슬롯에 들어가면 제자리 교체
2. 넘치면 **ISO 끝에 섹터 정렬로 추가** + 디렉터리 레코드 LBA/크기 갱신 + PVD
   `volume_space_size`(offset 80 LE / 84 BE) 갱신. **기존 파일 데이터는 건드리지 않음**

참고: JP ISO 의 `DIS2_PSP_DATA` (LBA 55,280, **262,144,000B 전부 0x00**)도 활용 가능한 빈 영역.
단 게임이 읽는지 미확인이므로 ISO 끝 추가가 더 안전하다.

### 인게임 검증 결과
빈출 대사 32건 번역 → **3,630회 적용**, 크기 +1,820B.
- 게임 정상 진입, 크래시 없음
- RAM에서 번역 문자열 확인 (`・돌아가기` 28회, `무덤이 있다.` 25회 등)
- **화면에서 선택지 "・돌아가기" 정상 표시 확인**

### 빌드
```
python tools/talk_inventory.py     # 고유 문자열 인벤토리
python tools/build_talk.py --iso   # 번역 적용 + 재패킹 + ISO 주입
```
번역 원본: `work/tr_talk*.py` — dict `T = {'원문일본어': '한글'}` (원문 문자열이 키)
→ 전 talk 파일의 모든 occurrence 에 일괄 적용되므로 **한 번 번역하면 중복 전부 반영**

### 번역 분량 (talk)
| 항목 | 수치 |
|---|---|
| 파일 | 17개 |
| 등장 | 63,126회 |
| **고유 문자열** | **9,828건** |
| **고유 일본어** | **150,931자** |
| 길이 | 중간값 32B, 평균 30B, 최대 54B |

산출물: `work/talk_inventory.tsv` (n, bytes, occ, jp, ko)

---

## 14. ★ 글리프 배치 개편 (v2) — 가나 보존 + 사용자정의 코드영역

초기 설계(한자·가나 전부 삭제, 한글 코드 0x88~)를 인게임 확인 후 두 가지 이유로 개편했다.

### 문제 1 — 미번역 일본어가 전혀 안 보인다
한자·가나를 다 지우니 번역 안 된 대사가 **아무것도 그려지지 않았다**(보존 기호만 조각 표시).
번역이 끝날 때까지 게임이 깨져 보이고, 부분 확인·부분 배포가 불가능하다.

**해결: 가나(히라가나 80 + 가타카나 82 = 162자)를 보존한다.**
- 미번역 대사가 일본어로 읽히므로 진행 중에도 게임이 정상적으로 보인다
- 어디가 미번역인지 한눈에 구분된다
- 버리는 것: 전각영숫자(62) · 그리스/키릴(7) → 번역에서 ASCII 로 대체

### 문제 2 — 한글 코드가 상용한자와 겹쳐 "엉뚱한 한글"이 나온다
한글을 `0x88~0x93` 에 두면 그 구간의 **원본 한자 1,480개**가 한글로 매핑돼
미번역 대사에 엉뚱한 한글이 섞인다(공백보다 더 혼란스럽다).
예: `気`(SJIS 8B43) → idx 2018 → 한글 구간 적중

**해결: 한글 코드를 SJIS 사용자정의(외자) 영역 `0xF0~0xFC` 로 이동.**
- 원본이 이 영역에 쓰는 글리프: **0개 (충돌 없음)** — 실측 확인
- `row = c1 - 0xC1` (c1 ≥ 0xE0 분기) → row 47..58, idx 9119..11404 < 11615 ✓
- 자간도 동일 15px (영문자 판정 A-Z/a-z 에 안 걸림)
- 예: `아델` = `F6E0 F28D`
- 미번역 한자는 이제 **깔끔하게 공백**

### 최종 배치 (gid 2591 까지 정확히 채움)
| gid | 내용 | 개수 |
|---|---|---|
| 1 ~ 131 | ASCII + lead 0x81 전각기호·버튼아이콘 (○△□×！？「」…) | 131 (원위치) |
| **132 ~ 2417** | **한글** (상용 2350 중 실사용 없는 64자 제외) | **2286** |
| 2418 ~ 2591 | 가나 162 + 기호 12 (─ ② ™ ® Ⅰ Ⅱ Ⅲ ♠) | 174 (이전) |

### 한글 음절 선정 (`tools/hangul_rank.py`)
자모 빈도 휴리스틱은 **쓰면 안 된다** — `꺾 끊 넓 몫 삶 싫 쏟 앓 얇 옮 옳 젊 짧 훑 핥 엌`
같은 실사용 음절을 잘라낸다(실제로 겪음).
대신 **실제 한국어에 없는 음절만** 명시 나열한 `RARE_POOL` 에서만 뺀다.
현재 제외 64자: `갊걺곬굻긺깖뀁뀄뀝끎낢…팖` (전부 실사용 없음, 풀 여유 26자)

**self-healing**: `bake_font.py` 가 `work/tr_*.py` 의 모든 번역에서 쓰인 음절을 `must` 로
강제 포함한다. 번역이 늘면 재베이크만으로 자동 반영되고, 누락은 `krtext.validate()` 가 잡는다.

### FontB0001.txp 는 글리프 페이지가 아니다
`f6` 필드(offset 0x0C)가 **스위즐 플래그**다. FontB0001 은 `f6=0`(비스위즐)이고
내용은 **UI 프레임·버튼 배경 도형**(말풍선, 버튼판, スキップ 라벨)이다. 한글용으로 못 쓴다.
| 파일 | f6 | 내용 |
|---|---|---|
| fontB.ftd / FontB0000.txp | 1 | 글리프 (스위즐) |
| FontB0001.txp | 0 | UI 도형 (비스위즐) |

### 캐릭터 이름 — 텍스트다 (이전 "이미지" 판단은 오류)
「アデル」이 가나 삭제 상태에서도 표시된 것을 근거로 "이미지"라고 적었으나 **틀렸다.**
이름은 텍스트로 저장돼 있다:
- `PSP_GAME/USRDIR/CHAR.DAT` (LBA 260,448 / 102,176B) — `アデル` 11회
- `START_JP.bin` 내부 — 105회 (대사 본문에 등장하는 것)

**남은 의문(미해결)**: 가나 글리프를 전부 지운 빌드에서도 대사창 이름표에 「アデル」이
표시됐다. 이름표가 별도 폰트 인스턴스(§5 의 fontinfo 3개, 코드 0x88xx/0x98xx/0x99xx 로
선택되는 gaiji 기구)를 쓰는지, 아니면 초상화 이미지에 이름이 함께 구워져 있는지 미확인.
→ `CHAR.DAT` 이름을 한글로 바꿔 화면이 바뀌는지 보면 결론이 난다.

### CHAR.DAT 포맷 (캐릭터·클래스 이름)
```
+0x00  u32 count (396)
+0x04  u32 count (중복)
+0x08  record[396], 각 0x102 (=258) 바이트
         +0x00   char[0x17] 이름   (NUL 종단, 최대 실측 16B)
         +0x17   char[0x17] 직업/칭호
         +0x2E~  나머지 스탯 등
검산: 8 + 396*258 = 102,176 = 파일 크기 ✓
```
**고정 길이 필드**라 이름은 23바이트(한글 11자) 이내로만 쓸 수 있다.
ISO 최상위 단일 파일이므로 아카이브 재빌드 없이 바로 교체 가능.

표본: `アデル`/`デビルバスター`, `ロザリー`/`魔王の娘`, `エトナ`/`魔神`, `ラハール`/`魔王`,
`フロン`/`堕天使`, `ティンク`/`珍妙なカエル` … 이름+직업 792개

---

## 15. 검증 스크립트

```
python tools/verify_iso.py
```
빌드된 ISO를 되읽어 한 번에 확인:
1. `START_JP.LZS` 해제 → 멤버 21개 추출
2. **무손상 검증** — 수정 대상 5개만 바뀌고 나머지 16개는 원본과 바이트 일치
3. `script00` 문자열 디코드 (한글/가나/기호 역변환)
4. `SCRIPTPACK` 언팩 → talk01 문자열 수 · 한글 포함 수
5. **오프셋 정합성** — 마지막 레코드가 파일 끝 −8 을 가리키는지

현재 결과: `전체 검증 OK`, talk01 문자열 4,063개 중 한글 포함 1,970개,
미번역 대사는 일본어로 정상 판독(`さぁ、選ぶがよい、死への旅路を……ッス。`)

---

## 16. InProgramTxtDB.dat — talk 와 동일 포맷 (규명 완료)

오랫동안 "포맷 미규명"으로 남겨뒀지만, **`talkXX_jp.dat` 와 완전히 같은 구조**였다.
`tools/talkfile.py` 가 그대로 동작한다.

```
+0x00  u32 count = 1988
+0x04  u32 count (중복)
+0x08  record[1988] x 0x20   (+0x00 = 테이블끝 기준 스트림 오프셋)
table_end = 8 + 1988*0x20 = 0xF888
이후    바이트코드 스트림, 문자열은 `01 <SJIS> 00` 인라인
```
- 무수정 재구성 == 원본 **바이트 일치** ✓
- 오프셋 정합성: `0xF888 + 마지막 field0 = 0x40782`, 파일 `0x40790` (말미 14B)
- 문자열 **4,716회 / 고유 2,911건 / 일본어 43,007자**
- 내용: NPC 대사, 아이템·스킬 설명 등

`tools/build_jp.py` 에 연결됨 — 번역 원본 `work/tr_iptxt*.py` (dict `T = {'원문':'한글'}`)

---

## 17. CHAR.DAT 번역 완료 (792건)

`work/tr_char1.py` (436 고유) → `tools/build_char.py --iso` → 적용 792회, 미번역 0종.
ISO 되읽기 검증: `아델`/`데빌 버스터`, `로자리`/`마왕의 딸`, `에트나`/`마신`,
`라하르`/`마왕`, `플로네`/`타천사`, `사무라이`/`검호` …

주의: 필드 폭 0x17(23B) 고정 → 한글 11자 이내. 초과 시 빌드가 예외로 막는다.

### 과거 가설: 대사창 이름표 출처 = `sys2.txp` (오판 — §24 참조)
`CHAR.DAT` 를 번역해도 이름표는 「アデル」로 남았다 → CHAR.DAT 가 출처가 아니다.
추적 결과 **`sys2.txp` 가 실제로는 TXP 가 아니라 또 하나의 캐릭터 DB** 였다.

```
sys2.txp  (START 멤버, 123,008B)  — 이름만 .txp, 내용은 캐릭터 DB
+0x00  u32 count = 500
+0x04  u32 count (중복)
+0x08  record[500] x 0xF6 (=246)
         +0x00  char[0x17] 이름
         +0x17  char[0x17] 직업/칭호
검산: 8 + 500*246 = 123,008 = 파일 크기 ✓
```
TXP 로 파싱하면 헤더가 500x0 / pb=26243 같은 쓰레기값이 나온다 — 그게 단서였다.

`CHAR.DAT`(396x0x102) 와 **구조는 같고 stride 만 다른 별개 테이블**이다.
- 공통 문자열 435건, `sys2` 전용 142건 (`ラズベリル` `マオ` `天使兵` `鬼道衆` 등)
- 이름/직업 각 0x17(23B) 고정 → 한글 11자 이내

**둘 다 패치해야 한다.** `tools/build_jp.py` 가 `work/tr_char*.py` 를 읽어 sys2 를,
`tools/build_char.py` 가 같은 번역으로 CHAR.DAT 를 처리한다.
현재 sys2 **1,000회 적용 / 미번역 0종**, CHAR.DAT **792회 적용 / 미번역 0종**.

**교훈**: 확장자를 믿지 말 것. `.txp` 인데 TXP 가 아니고, `InProgramTxtDB.dat` 는
talk 스크립트 포맷이었다. 헤더 검산(`8 + count*stride == 파일크기`)이 가장 확실한 판별법이다.

### 미검증으로 남은 것
이름표가 실제로 한글로 바뀌었는지 **인게임 확인을 못 했다.** 디버거 `input.buttons.press` 와
키보드 SendKeys(Z=○, X=×, Enter=START — `controls.ini` 의 `1-54`/`1-52`/`1-66`) 모두
타이틀 메뉴에서 선택이 먹지 않아 게임 진입에 실패했다.
→ 사람이 직접 조작해 대사 장면을 확인해야 한다. 데이터 검증(ISO 되읽기)은 통과한 상태.

---

## 18. 전체 진행 현황

| 소스 | 고유 문자열 | 일본어 자수 | 상태 |
|---|---|---|---|
| `script00.dat` | 1,093 | 7,920 | **번역 완료 · 인게임 검증** |
| `CHAR.DAT` | 436 (적용 792) | — | **번역 완료 · ISO 검증** |
| `InProgramTxtDB.dat` | 2,911 | 43,007 | 주입 경로 완성, 번역 미착수 |
| talk×17 | 9,828 | 150,931 | 주입 경로 완성 · 인게임 검증, 32건 번역 |
| script×17 | 23 | 366 | 다른 포맷, 미착수(텍스트 거의 없음) |

**주입 경로는 전부 뚫렸다.** 남은 것은 번역 분량 = 고유 약 12,700건 / 약 19만 자.

### 빌드 순서
```
python tools/bake_font.py          # 폰트(번역 음절 자동 반영)
python tools/merge_tr.py           # script00 번역 병합·검증
python tools/build_jp.py  --iso    # START (폰트+script00+InProgramTxtDB)
python tools/build_talk.py --iso   # SCRIPTPACK (talk 대사)
python tools/build_char.py --iso   # CHAR.DAT (캐릭터·클래스명)
python tools/verify_iso.py         # 정적 검증
```

---

## 19. 과거 조사: 대사창 화자 이름표 출처 (최종 해결은 §24)

`sys2.txp`(500레코드) 와 `CHAR.DAT`(396레코드) 를 **둘 다 번역·적용**했는데도
대사창 이름표는 여전히 원문(「ママ」「アデル」)으로 나온다.

### 확인된 사실
1. **패치는 메모리에 반영됐다** — RAM 검색 결과:
   - `데빌 버스터`(한글) 3회 / `デビルバスター`(원문) **0회** ← 완전 대체됨
   - `아델`(한글) 16회, `엄마`(한글) 1회
   - 그런데 `アデル`(원문) 159회, `ママ`(원문) 43회가 **다른 출처에 남아 있다**
2. **화자는 숫자 ID로 지정된다** — talk 바이트코드에서 대사 직전:
   `... 07 02 01 06 ef 03 0d f0 03 08 07 00 01 <문자열>`
   `06 ef 03` = 0x3EF(1007), `0d f0 03` = 0x3F0(1008) 같은 ID 값
   → 이름 문자열은 이 ID 로 인덱싱되는 별도 테이블에 있다
3. **다음은 출처가 아니다 (확인 완료)**
   | 후보 | 결과 |
   |---|---|
   | `sys2.txp` / `CHAR.DAT` | 번역 적용됨에도 화면 불변 → 아님 |
   | talk 파일 단독 문자열 | `ママ`/`アデル` 단독 문자열 **0개**. 전부 대사 본문 중간 |
   | `InProgramTxtDB.dat` | 단독 이름 0개. 레코드 영역(<0xF888)에도 0개. 전부 대사 본문 |
   | `anm0000.dat` | `アデル` 48회지만 `ママ` 0회 → 엔딩 타이핑 텍스트 |
   | ISO 최상위 (ANMPACK/CUTINPACK/MAPPACK/TXPPACK/SHORTANIMEPACK/MODULE/NAME/DUNGEON) | `アデル` **0회** |

### 남은 가설
- 이름 테이블이 **EBOOT 내부**에 하드코딩되어 있다 (JP EBOOT 은 KIRK 암호화, RAM 덤프로 접근 가능)
- 또는 `01 <SJIS> 00` 이 아닌 **다른 문자열 형식**(고정폭·다른 접두바이트)으로 저장돼
  현재 탐지기가 못 찾는다

### 추적 방법 (다음에 할 것)
대사 장면을 띄운 상태에서 RAM 의 `ママ` 43개 주소를 모두 나열하고,
**렌더러가 읽는 주소를 memory read watchpoint 로 잡는다.** `tools/ppsspp_dbg.py` 의
`memory.breakpoint.add {read:true}` 사용. 히트 PC 를 `memory.disasm` 으로 역추적하면
어느 테이블에서 오는지 확정된다. (§8 의 JIT 오염 주의 — 코드 판독은 API 로)

### 영향 범위
**이름표 한 줄뿐이다.** 대사·UI·아이템·클래스명은 전부 정상 경로로 패치된다.
전체 번역 진행을 막지 않으므로 별건으로 처리한다.

---

## 20. 대량 번역 파이프라인 (서브에이전트 병렬)

### 구조
```
work/GLOSSARY.md          공용 번역 규칙·용어집 — 모든 에이전트가 먼저 읽는다
work/chunks/*.txt         원문 청크 (각 350건, 한 줄에 하나)
work/tr_iptxt_a*.py       InProgramTxtDB 번역 결과
work/tr_talk_b*.py        talk 번역 결과
tools/qa_tr.py            품질 검사 (키정합성·인코딩·서식자·자리표시자·용어)
```

### 일관성 확보 (병렬 번역의 최대 리스크)
`GLOSSARY.md` 에 다음을 못박아 모든 에이전트가 동일하게 쓰도록 했다.
- 고정 용어 100여 개 (베이스 패널 / 아이템계 / 다크 태양 / 마체인지 / 계적단 …)
- 말투 구분: `ッス`→`~스`, `ニャ`→`~냐`, `みャ`→`~먀`
- 이노센트 `○○屋` → `○○꾼` (재판꾼 / HP꾼 / 독꾼 …)
- 사용 가능 문자 **147자 전각기호 전량 명시** (초기에 목록이 불완전해 에이전트가
  `♪ 【】 ※` 를 불필요하게 제거한 사례 발생)

### ★ 겪은 함정 3개

**1. 선행바이트 범위** — `talkfile._sjis_runs` 가 `0xE0~0xEF` 만 2바이트 선행으로 인정해
한글 코드(`0xF0~0xFC`)로 번역된 파일의 문자열을 **탐지하지 못했다.**
검증기가 talk01 문자열을 4,063개 → 728개로 잘못 보고했다.
→ 범위를 `0xE0~0xFC` 로 확장. 원본 왕복 회귀검사 17/17 통과 확인.

**2. self-healing 반쪽 작동** — `bake_font.py` 가 `tr_batch*` / `tr_talk*` 만 읽어
`tr_char*` / `tr_iptxt*` 에서 새로 쓰인 음절이 폰트에 안 들어갔다(`엾` 누락).
→ 4개 패턴 전부 읽도록 수정. 사용 음절 853 → 1,027자.

**3. 전각 `％` 오탐** — 원문 `７０％` → 번역 `70%` 는 정상인데 서식지정자 검사가
개수 불일치로 잡았다. → 비교 전에 원문의 `％` 를 `%` 로 정규화.

### 상용한글 밖 음절
`햣` `큿` 같은 조합은 KS X 1001 에 없어 **인코딩 불가**. 의성어에서 자주 나온다.
→ `햐` `쿡` 등 표준 음절로 대체. `krtext.validate()` 가 빌드 시 잡아준다.

### 원문 근거로 정정한 사례
`斧雪` 를 `후세츠` 로 잡았으나, 에이전트가 원문 `斧雪（ふぶき）`(talk06) 를 찾아내
**`후부키`** 로 정정. 후리가나가 있으면 그것이 정답이다.

---

## 21. 고정 레코드 DB 6종 (뒤늦게 발견한 2,044건)

talk / InProgramTxtDB / script00 / sys2 를 다 끝낸 뒤, `jp/start` 에서 **아직 손대지 않은
데이터 파일 8개**를 훑어 텍스트를 찾았다. 6개가 번역 대상이었다.

### 포맷 (6개 공통)
```
+0x00  u32 count
+0x04  u32 count (중복)
+hdr   record[count]   각 rs 바이트
```
문자열 필드는 레코드 내 **고정 오프셋 · 고정 폭**. `tools/recdat.py` 의 SPEC 에 등록.

| 파일 | hdr | rec | 텍스트 필드(오프셋/폭) | 고유 | 내용 |
|---|---|---|---|---|---|
| HABIT.dat    | 8  | 120 | 0x00/21, 0x15/87        | 1141 | 무기·기술명 + 설명 |
| dungeon.dat  | 8  | 64  | 0x00/64                 | 122  | 이노센트·던전 이름 |
| charhelp.dat | 8  | 80  | 0x00/22                 | 132  | 맵 이름 |
| magic.dat    | 8  | 152 | 0x00/50, 0x32/50, 0x64/50 | 364 | 마법·특수 설명 3줄 |
| mitem.dat    | 16 | 104 | 0x08/24, 0x20/72        | 172  | 음악방 항목 + 설명 |
| music.dat    | 8  | 140 | 0x28/47, 0x57/53        | 113  | 의회 의제 + 효과 |

검증식: `hdr + count*rec` == 파일크기 (또는 +8 패딩). 6개 전부 성립.

### ★ talk 계열과 결정적으로 다른 점 — 길이 제한
필드 폭이 고정이므로 **번역문이 폭을 넘으면 다음 필드를 침범한다.** 종단 NUL 1바이트를
빼면 가용은 `폭-1`. 한글은 2바이트/음절이므로:
- 예산 20 → 한글 10자 (HABIT 무기명 582건이 여기 해당, 가장 빡빡하다)
- 예산 49 → 한글 24자 (magic 설명 364건)

`recdat.put()` 이 초과 시 예외, `tools/qa_rec.py` 가 번역 단계에서 미리 잡는다.
청크는 `<예산>	<원문>` 형식으로 만들어 에이전트에게 줄마다 예산을 알려준다.

### 오검출 주의
필드 자동 탐지는 4바이트 정수 필드를 문자열로 잡는다(music.dat +0x00/+0x04/+0x08 등).
SPEC 에는 **실제 텍스트 필드만** 등록했다. 또 `comb.dat`(208건)·`WISH2.dat` 는 전부
바이너리 오검출이라 제외했다.

### 슬롯 여유
이 6개는 크기가 변하지 않지만 한글이 한자보다 압축률이 낮아 START_JP.LZS 가 커진다.
반영 전 4878415B / 슬롯 5013504B (여유 135,089B). 빌드 시 초과 여부 확인 필요.

---

## 22. 전수 한자 스캔 (`tools/scan_kanji.py`) — 번역률 100% 로도 못 잡는 것

폰트에서 한자 셀을 전부 지웠으므로 **한자 코드가 하나라도 남으면 인게임에서 빈칸**이 된다.
인벤토리 기준 번역률이 100% 여도 (a) 인벤토리 자체가 문자열을 놓쳤거나
(b) 다른 포맷에 문자열이 있으면 그대로 남는다. 그래서 빌드된 ISO 를 되읽어
바이트 수준으로 훑는 스캐너를 따로 만들었다.

판정: 선행바이트 `0x88~0xEF` = 지운 한자 영역. (`0x81` 보존기호 / `0x84`,`0x87` 이전한
가나 / `0xF0~0xFC` 한글 은 정상)

### 이 스캔이 실제로 잡아낸 것

**1. script00 시스템 메시지 8건 누락 (실제 결함)**
`textio.dump()` 가 `raw.decode('shift_jis')` 를 strict 로 쓰고 있었다. `⑪`(U+246A)
`④`(U+2463) `㍉`(U+3349) 는 **cp932 전용 NEC 특수문자**라 strict shift_jis 로는
디코드에 실패 → `text=None` → 인벤토리에서 조용히 탈락했다.
탈락한 것이 하필 **메모리스틱 삽입·용량부족 등 세이브 시스템 메시지**였다.
→ `dump()` 를 cp932 우선으로 고치고 `work/tr_batch5.py` 로 번역.
→ 이 글리프들은 lead 0x87 보존 구간이라 그대로 쓸 수 있다(⑪=874A, ④=8743, ㍉=875F).

**2. `d<한자>` 6건 = 문자열이 아니라 바이트코드 (오검출)**
talk08/09/11/12 에 `d孛` `d匠` `d蓉` `d摺` `d冰` `d勃` 가 남았다. 주변 바이트를 보면
```
... 0b XX XX | 01 | 64 YY ZZ | 00 0b 00 00 00 ...
앞쪽: ff ff 00 00 00 0e ff ff 00 00 00 18 ...   (테이블)
```
`01 <str> 00` 프레이밍에 우연히 맞은 **바이트코드**다. 정상 대사는 앞에 `81 42 00`
(、。) 같은 문장부호로 끝난다. → **건드리면 스크립트가 깨진다.** 딕트에서 제외가 정답.

**3. 짧은 문자열 오검출 감사 결과: 없음**
같은 오검출이 다른 곳에서 일어나 바이트코드를 텍스트로 덮어썼을 위험을 확인했다.
번역 대상 9,822건 중 원문 4바이트 이하는 6건뿐이고 전부 명백한 일본어
(`はい` `へ？` `！！` `ん？` 전각공백)였다. 안전.

### bake_font self-healing — 세 번째 재발
`used_syllables()` 가 패턴을 열거하는 방식이라 새 파일 계열이 생길 때마다 누락됐다
(`tr_char*`/`tr_iptxt*` 때 1회, `tr_rec*` 때 1회 = 총 2회 사고, 각각 50자 내외 누락).
→ 열거를 없애고 `work/tr_*.py` 전부 읽도록 고정. **패턴을 다시 열거하지 말 것.**

### 전각 ％ 양쪽 정규화
`build_talk.py` 가 JP 쪽만 `％`→`%` 정규화하고 KO 쪽은 안 해서, 원문의 전각 `％` 를
그대로 살린 정상 번역이 불일치로 잡혀 빌드가 멈췄다. 양쪽 다 정규화해야 한다.

---

## 23. ★ 인게임 검증에서 드러난 렌더러 제약 (2026-08-22~23)

정적 검증(`verify_iso.py`, `scan_kanji.py`)이 전부 OK 인 상태에서 **인게임 화면을 보고서야**
드러난 결함들이다. 파일·바이트·인코딩이 모두 정상인데 화면이 깨지는 부류라, 정적 검증으로는
절대 잡히지 않는다. 반드시 실제로 대사를 띄워 봐야 한다.

### 23-1. 해결: 한글 코드의 후행바이트 제약

**증상** 글자 사이에 알파벳이 끼어든다 — `해f도` `제R논` `필X요` `명g 년` `하려면c,`

**원인** 게임이 그 2바이트 코드를 그린 뒤 포인터를 **1바이트만 전진**시켜, 후행바이트를
ASCII 로 한 번 더 그린다. 파일 바이트는 정상(`fb 66`=해, `f8 52`=제)이었다.

| 후행바이트 | 표본 | 깨짐 |
|---|---|---|
| >= 0x80 | 27자 | 0 |
| < 0x80  | 13자 | 6 (0x4D 0x52 0x58 0x63 0x66 0x67) |

같은 선행바이트라도 `0xFB5D`(하)는 정상, `0xFB66`(해)는 깨진다 -> 선행이 아니라 후행 값 문제.
`<0x80` 이 전부 깨지는 것은 아니지만 **깨지는 것은 전부 `<0x80`** 이라 그 범위를 통째로 피한다.

**대응** `krfont.n_to_code` 를 `0x80 + n%125` 로 바꿔 후행을 `0x80~0xFC` 만 쓴다.
용량이 2,279 -> **1,625자**로 줄어(선행 0xF0~0xFC 13개 x 후행 125개) 실사용 1,187자 + 여유 438자.
`hangul_rank.pick()` 도 "희귀한 것만 빼기"에서 "실사용 강제 + 말뭉치 자모빈도 순 채우기"로 교체.

### 23-2. 해결: 글리프 없는 ASCII / 공백 자간

원본 폰트에 `$ & ' * : < > ? @ \ ^ ` q v z |` 의 글리프가 **아예 없다**(맵이 gid 0).
번역이 `?` 1,984회 · `~` 538회를 쓰는데 전부 빈칸으로 나왔다.

**★ 여기서 회귀를 만들었다** — 빈 gid 에 그 글리프들을 구워 넣었더니, 대사창에 `|` `'` 같은
쓰레기가 드러났다. 원본이 글리프를 비워둔 것은 누락이 아니라 **바이트코드에 섞인 ASCII 값을
화면에서 숨기는 장치**다(해당 대사 바이트코드에 실제로 `[0b]z'[00][0b]X'[00]` 가 있다).
-> 글리프 베이크는 철회하고, 번역문의 그 문자들을 **전각**(`？ ～ ： ＆ ＜ ＞ ’`)으로 치환.
전각 쪽은 lead 0x81 보존 구간이라 원본 글리프가 살아 있다.

`krtext.validate()` 가 ASCII 0x20~0x7E 를 무조건 통과시키고 있었던 것도 고쳤다(맵을 읽어
gid 0 인 ASCII 를 거부). 이 구멍 때문에 물음표 1,984개가 QA 를 그냥 지나갔다.

### 23-3. ★ 핵심: 대사 렌더러는 모든 문자가 2바이트라고 가정한다

**증상** `마왕` -> `규`, `……` -> `…c…`, `ww を ％` 같은 쓰레기, 빈칸, 줄 넘침

**결정적 증거** 작업 버퍼(`0x08be307c` 부근)에서 1바이트 밀린 사본을 발견했다.
```
… 제논을 소환할 수[00]자[ce]～. 이런 데 처박히면,
```
`엄마～．…` 가 `자[ce]～．…` 로 들어 있다 — `[엄][마][～]` = `XX YY | f4 ce | 81 60` 인데
버퍼는 `YY f4 | ce | 81 60` 으로 시작한다. **문자 경계를 무시하고 고정 바이트 오프셋으로
줄을 자른 결과.** 일본어는 거의 전부 2바이트라 어디서 잘라도 경계에 맞지만, 우리 번역의
1바이트 ASCII(공백·`.`·`,`·`!`·숫자)가 이후 오프셋을 홀수로 밀어버린다.

**검증** 한 장면 36건을 완전 전각화하니 `너희들，　악마잖니，` 가 **완벽하게** 나왔고,
ASCII 숫자만 남은 줄은 그 자리만 빈칸이 되고 뒤 텍스트는 온전했다.

**결론: 대사(talk / InProgramTxtDB)는 모든 문자가 2바이트여야 한다.**
`build_talk.py` 의 `FW_MAP` 이 ` . , ! ? ~ : ; ( ) - /` 를 전각으로 바꾼다
(`work/fw_keys.txt` 에 적은 원문만 적용 — 전체 적용은 크기 문제로 아직 불가).

### 23-4. 배제된 가설 (다시 시도하지 말 것)

| 가설 | 반증 |
|---|---|
| 번역문에 가나 혼입 | 전수 검사 0건 |
| 탐지·치환 범위 불일치 | talk 문자열 63,129개 중 불일치 0건 |
| 레코드 오프셋 보정 오류 | 970개 레코드 전부 같은 명령을 가리킴(앞 4바이트 일치) |
| 레코드에 문자열 길이 필드가 있다 | 전 필드 상관 0/769 |
| 게임이 원문 길이만큼 읽는다 | 문제 문자열이 번역이 원문보다 **1바이트 긴** 경우에도 발생(43B vs 42B) |
| 한글 코드표 문제 | 코드가 전혀 다른 세 빌드에서 증상 위치가 동일 |
| **문자열 단위 짝수 길이 패딩**(+32KB, 값싼 대안) | **실패.** 같은 `……` 가 문자열 내 위치에 따라 깨지고 안 깨진다 |

### 23-5. SCRIPTPACK 크기 — ★ 아래 결론은 틀렸다 (23-10 참조)

전체 대사 전각화 = **3,484,323B**. 원본 3,362,798B / 원래 슬롯 3,375,104B.

**실측표 (전부 인게임 부팅 시험)**

| SCRIPTPACK | ISO 배치 | 결과 |
|---|---|---|
| 3,229,556B | START 이동 | OK |
| 3,356,194B | 원래 | (현재 빌드) |
| 3,366,963B | 원래 | **OK** ← 확인된 최대치 |
| 3,373,427B | START 이동 | 뻗음 |
| 3,387,151B | START 이동 | 뻗음 |
| 3,484,323B | START 이동 | 뻗음 |
| 3,431,539B | **ISO 끝 재배치** | "Now Loading" 정지 |
| 3,426,749B | **ISO 끝 재배치** | "Now Loading" 정지 |

**확정**
1. **ISO 끝 재배치는 불가** (2회). `isopatch.replace` 는 디렉터리 레코드 LBA/크기(LE+BE)와
   PVD volume_space_size 를 모두 정확히 갱신하고 `verify_iso.py` 도 정상 판독하는데도 안 된다.
   Joliet 보조 디스크립터는 없다(섹터 16 PVD, 17 종단자뿐).
2. **크기가 원본을 넘는 것 자체는 문제없다** — 3,366,963B(원본 +4,165B) 부팅·진행 확인.
3. **슬롯 경계를 넓혀도 소용없다.** ISO 배치는 이렇게 붙어 있고 유휴가 176,528B 흩어져 있다.
   ```
   SCRIPTPACK [248176-249821] 3,366,963  |유휴 6,144B|
   START_JP   [249824-252190] 4,845,117  |유휴 167,936B|  START_VM2_JP [252272-]
   ```
   START_JP 를 249886 으로 밀어 SCRIPTPACK 가용을 3,502,080B 로 넓혔지만 3,373,427B 에서
   뻗었다. **게임이 자체 버퍼로 제한하며, 그 크기가 원래 슬롯(3,375,104B) 근처로 보인다.**
   -> 경계는 원복했다(`build_jp.JP_ISO_LBA=249824`, `build_talk.ISO_NEXT=249824`).
   START 이동 자체는 무해했다(3,229,556B 로 부팅 OK). 이득이 없어 되돌린 것.

**주의** 3,373,427B 가 뻗은 것은 **이동 배치**에서였다. 원래 배치에서 그 크기가 되는지는
아직 시험하지 않았다. 상한을 정확히 좁히려면 원래 배치로 3,367,000~3,375,000 구간을 재야 한다.

**부족분** 전체 전각화까지 약 **118,000B** (3,484,323 - 3,366,963).

**아직 시도하지 않은 확보 수단**
- **멤버 정렬 패딩 회수 — 36,765B.** `scriptpack.pack` 이 34개 멤버를 0x800 정렬로 넣는다
  (실데이터 3,326,033B / 파일 3,362,798B). 헤더에 각 멤버 오프셋이 들어 있으니 정렬 없이
  붙여도 읽힐 가능성이 있다. 부팅 시험 한 번으로 갈린다. **미검증.**
- **번역문 축약 — 나머지 약 81,000B.** SCRIPTPACK 내 한글 총량의 약 5%.
  문자열 하나가 평균 6.4곳(63,120회 / 고유 9,822건)에 쓰이므로 **빈출 문자열 1바이트 축약이
  6.4바이트 절약**이다. 등장 가중 정렬로 상위부터 다듬는 것이 효율적.
- **전각화 최소 집합 규명.** 지금은 ` . , ! ? ~ : ; ( ) - /` 전부를 전각으로 바꾼다. 공백이
  압도적 비중(고유 31,167회)이라 공백만으로도 초과하지만, 렌더러가 요구하는 최소 조건을
  디버거로 정확히 알면 비용이 줄 수 있다. **미규명.**

### 23-6. 남은 시각 결함
- **전각 숫자·영문 글리프가 없다.** `collect_kana` 가 0xFF01~0xFF5E(전각영숫자)를 버리므로
  `１` `Ａ` 글리프가 지워졌다. 그래서 대사의 `1년`, `TW`, `GZ256` 은 ASCII 로 남아 있고
  그 자리가 빈칸으로 나온다. 빈 gid 1757~2410(654칸)에 구워 넣으면 된다.
  단 23-2 의 교훈대로, 바이트코드에 그 2바이트 코드가 섞여 있으면 쓰레기가 드러날 수 있다.
- **이름표 `ママ`** — 19절과 동일. 이번 문제와 무관하며 여전히 미해결.

### 23-7. ★ 다음 조사 진입점 — 줄 자르기/복사 루틴

6-B 절에 **JP 그리기 함수가 이미 규명되어 있다**: `0x088A8748` (루프 머리 `0x088A8BEC`).
그리고 그 함수는 자간을 고를 때 `(0x41<=c<0x5B) || (0x61<=c<0x7B)` 로 **ASCII 영문을 따로
판정**한다 — 즉 **그리기 루프 자체는 1바이트 문자를 정상 처리한다.**

따라서 23-3 의 "2바이트 가정"은 그리기 루프가 아니라 **그 앞단, 원문을 작업 버퍼로 옮기며
줄을 나누는 루틴**에 있다. 작업 버퍼는 `0x08be307c` 부근에서 관측됐다(1바이트 밀린 사본).

**조사 방법**
1. 작업 버퍼 주소를 찾는다 — 화면에 나올 문자열을 `krtext.encode` 로 인코딩해 RAM 검색.
   talk 파일 사본이 아니라 **사본이 여러 개 나오는 쪽**이 작업 버퍼다.
2. 그 주소에 **쓰기(write) 브레이크포인트**를 걸고 대사를 한 줄 넘긴다.
   -> 복사 루틴의 PC 가 잡힌다. (읽기 브레이크포인트는 talk 원본에 걸어도 `hits: 0`)
3. `memory.disasm` 으로 그 루틴을 읽어, 몇 바이트 단위로 전진하며 어디서 줄을 끊는지 본다.

이걸 알면 **전각화 최소 집합**이 나온다. 지금은 ` . , ! ? ~ : ; ( ) - /` 를 전부 전각으로
바꿔 약 118,000B 가 부족한 상태다. 예컨대 "줄 길이가 짝수 바이트여야 한다"가 아니라
"줄 시작 오프셋이 짝수여야 한다" 같은 조건이면, 공백만 전각으로 하거나 특정 위치만
맞추는 식으로 비용이 크게 줄 수 있다.

**참고** 그리기 함수 `0x088A8748` 에 코드 브레이크포인트를 걸면 문자 단위로 멈추므로
호출 스택(ra)을 타고 올라가 상위 루틴(줄 나누기)을 찾는 것도 가능하다.

### 23-8. 작업 방식에서 얻은 교훈
- **잘 되는 빌드를 실험으로 갈아치우지 말 것.** 전각화가 성공한 상태를 지우고 값싼 대안
  (짝수 패딩)을 시험해 결과가 더 나빠졌다. 실험은 다른 장면/사본으로 해야 한다.
- **한 번에 하나만 바꿀 것.** START 이동과 SCRIPTPACK 증량을 동시에 바꿔서, 뻗었을 때
  어느 쪽이 원인인지 몰라 시험을 두 번 더 했다.
- **세이브 스테이트로 확인하지 말 것.** 스테이트에는 이전 빌드의 텍스트가 RAM 이미지로
  들어 있어 폰트·코드표 변경이 반영되지 않는다. 새로 부팅해서 진행해야 한다.
- 대사 렌더러는 이미 그려진 텍스트를 재읽지 않으므로, 메모리 읽기 브레이크포인트는
  **아직 그려지지 않은** 다음 문자열에 걸어야 한다. talk 파일 사본에 걸어도 안 맞는다
  (게임은 별도 작업 버퍼에서 읽는다 — `hits: 0` 으로 확인).

### 23-9. 디버거
`ppsspp.ini` 의 `RemoteDebuggerOnStartup = True` 로 **이미 켜져 있다**(포트 4543).
연결이 거부되면 PPSSPP 가 실행 중이 아닌 것이다. 설정을 바꿀 필요 없다.
`tools/ppsspp_dbg.py` 로 `memory.read` / `memory.breakpoint.add` / `cpu.status` /
`memory.disasm` 사용 가능. 입력 전송(`input.buttons.*`)은 이 게임에서 먹히지 않는다.

### 23-10. ★ 정정 — "총 파일 크기 상한"은 없다

23-5 의 결론은 **틀렸다.** 오류의 원인은 실험 설계다 — **총 크기를 단독으로 바꾼 적이 없다.**
모든 시험에서 "멤버 내용 전각화"와 "총 크기 증가"를 동시에 바꿨고, 뻗으면 총 크기 탓으로 돌렸다.
23-8 에 "한 번에 하나만 바꿀 것"을 적어놓고 정작 이 지점에서 적용하지 못했다.

**뒤집은 실험** EBOOT 의 팩 로더는 SCRIPTPACK.DAT 전체를 고정 버퍼에 올리지 않는다 —
헤더와 엔트리 표만 읽고 멤버를 오프셋으로 연다. 그래서 **정상 팩 내용을 그대로 두고 파일 뒤에
0 만 붙여 3,484,323B 로 만들고** START_JP 를 안전한 다음 슬롯으로 옮겼더니 **정상 부팅했다.**

-> **총 파일 크기 상한 없음. ISO 슬롯 상한 없음. 전체 버퍼 상한 없음.**
-> 23-5 표의 "뻗음" 사례들은 전부 전각화된 **내용**이 들어간 빌드였다. 크기는 무죄다.

**ISO 끝 재배치 실패(2회)는 여전히 유효한 관측**이지만, 그 빌드들도 전각화 내용이 들어 있었으므로
재배치 자체가 원인이라고 단정할 수 없다. 재배치 + 0 패딩만으로 다시 시험해야 한다.

### 23-11. 멤버별 크기 — ★ 근사였다 (진짜 원인은 23-12)

전각화 시 **모든 talk 멤버가 원본 크기를 초과**한다(멤버당 +4,578 ~ +8,885B).
0 패딩 실험은 어떤 멤버도 키우지 않으므로, 이 차이가 유일하게 남은 구조적 차이다.

| 멤버 | 원본 | 현재(정상) | 전각화 | 전각 Δ |
|---|---|---|---|---|
| talk01 | 202,734 | 194,420 | 211,619 | +8,885 |
| talk03 | 194,605 | 186,539 | 202,685 | +8,080 |
| talk13 | 186,642 | 179,113 | 194,709 | +8,067 |
| talk05 | 190,008 | 182,007 | 198,016 | +8,008 |
| … | | | | |
| talk20 | 124,700 | 118,393 | 129,278 | +4,578 |
| **합계** | 3,326,033 | 3,198,624 | 3,445,259 | +119,226 |

정상 동작하는 모든 빌드는 전 멤버가 원본보다 **작았다**(현재 Δ 전부 음수).

**분리 실험** 번역은 그대로 두고 `talk01_jp.dat` 스트림 말미에만 0 을 11,686B 덧붙여
그 멤버만 원본을 초과시킨다(문자열 무변경, 총 크기 96.28%). talk 포맷은 마지막 레코드 뒤에
꼬리 패딩이 있으므로(원본도 8바이트) 안전하다.
**분리 실험 결과: 뻗었다.** 번역 무변경 + `talk01_jp.dat` 만 0 패딩으로 원본 초과(+11,686B),
총 크기 96.28% -> **부팅 실패.**

```
총 파일 크기      상한 없음      (0 패딩 3,484,323B 부팅 OK)
멤버별 크기        원본 이하 필수  ← 확정
```

정상 동작한 모든 빌드가 전 멤버 원본 미만이었던 것과 정확히 일치한다.
멤버 하나만 초과해도 죽으므로, **멤버별 예산을 개별로 지켜야 한다.**

**예산** `B_m = 원본크기_m - 무변환크기_m`

| 멤버 | 예산 | 전각화 전체 비용 | 충족률 |
|---|---|---|---|
| talk01 | 8,314 | 17,199 | 48% |
| talk06 | 8,376 | 16,054 | 52% |
| talk05 | 8,001 | 16,009 | 50% |
| talk20 | 6,307 | 10,885 | 58% |

즉 지금 번역문 그대로는 **전각화를 절반 정도만** 넣을 수 있다. 전량 적용에는 멤버당
약 4% 축약이 필요하다(talk06: 초과 7,678B / 196,682B = 3.9%). 멤버들이 문자열을 공유하므로
**고유 번역문을 4% 다듬으면 전 멤버가 함께 줄어든다.**

**도구** `tools/pick_fw.py` — 멤버별 예산을 넘지 않는 최대 집합을 골라 `work/fw_keys.txt` 에 쓴다.
비용 오름차순 그리디(개수 최대화). 안전 여유를 남긴다.

(원래 예측)
- 뻗으면 -> **멤버별 크기 제약 확정.** 전각화 비용을 멤버 안에서 상쇄해야 한다.
  각 멤버 약 4% 축약이면 맞는다(예: talk06 초과 7,678B / 멤버 196,682B = 3.9%).
- 정상이면 -> 크기 계열은 전부 무죄. 남는 것은 전각화된 **문자열 내용** 자체다.
  그때는 전각 코드 중 특정 값(예: 전각공백 0x8140)이나 문자열 길이 분포를 의심해야 한다.

### 23-12. ★★ 진짜 원인 — talk 공용 버퍼 `0x318F8`

**제한은 talk 파일을 올리는 공용 버퍼 크기 `0x318F8` = 203,000B 다.**
SCRIPTPACK 총 크기와는 무관하고, 멤버별 "원본 이하"도 정확한 기준이 아니었다 —
talk01 원본이 202,734B 로 203,000B 바로 아래여서 두 기준이 구분되지 않았을 뿐이다.

```
전각화 talk01 = 211,619B  > 203,000B  -> 오버플로
전각화 talk06 = 204,360B  > 203,000B  -> 오버플로
```

버퍼를 `0x36000`(221,184B)으로 늘리자 **전량 전각화 ISO 에서 「새로 시작」 정상 통과**,
새 talk 버퍼가 `0x099E5A34` 에 실제 재할당된 것도 확인됐다.

**필요한 EBOOT 패치 9워드** (`ori` 즉치값 0x18F8 -> 0x6000)

```
0887A0BC  344718F8 -> 34476000
08892A80  344518F8 -> 34456000
08928F80  344718F8 -> 34476000
0892A35C  344718F8 -> 34476000
0892A39C  344718F8 -> 34476000
0892A640  344718F8 -> 34476000
0892A680  344718F8 -> 34476000
0892B57C  344718F8 -> 34476000
0892B5BC  344718F8 -> 34476000
```

**생성자 실행 전, 즉 완전 재부팅 시점부터 활성화해야 안전하다.**
(버퍼를 잡는 코드가 이미 돌아간 뒤에 값을 바꾸면 크기 불일치가 남는다.)

### 23-13. 배포 방식 — EBOOT 이 암호화되어 있다

`/PSP_GAME/SYSDIR/EBOOT.BIN` 은 `~PSP` 매직 = **암호화**. 9워드를 ISO 에 직접 쓸 수 없다.
`BOOT.BIN` 은 리테일 관례대로 전체가 0 이다.

| 방법 | 내용 | 제약 |
|---|---|---|
| **A. 데이터만으로 회피** | 각 talk 멤버를 203,000B 이하로 유지 | EBOOT 무패치. 전량 전각화 불가 — `tools/pick_fw.py` 로 81.1% 까지. 100% 는 번역문 약 4% 축약 필요 |
| **B. CWCheat** | 9워드를 치트로 적용 | 사용자가 치트 활성화 + 치트파일 설치 필요. 완전 재부팅부터 적용해야 함 |
| **C. 복호 EBOOT 교체** | PPSSPP 로 복호 EBOOT 을 덤프해 9워드 패치 후 ISO 에 되넣기 | PPSSPP 는 평문 ELF 를 EBOOT.BIN 으로 받아들인다. 치트 불필요·자기완결적. 실기에서는 못 쓴다(에뮬 전용). ISO 슬롯 여유 있음(EBOOT 1,611,136B / 슬롯 1,671,168B) |

에뮬레이터 전용 배포이므로 **C 가 가장 깔끔하다** — 사용자 설정 변경 없이 ISO 하나로 끝난다.
단 복호 덤프를 켜는 것은 PPSSPP 설정 변경이라 허락이 필요하다.

**A 는 B/C 와 배타적이지 않다.** 어느 쪽으로 가든 `pick_fw.py` 는 남겨둔다 —
EBOOT 패치가 없는 환경(치트 미적용 등)에서도 81% 는 정상으로 보이는 것이 안전판이 된다.

### 23-14. ★ C안(평문 ELF 교체) 정밀 검증 — 아직 불가

C안의 원리는 맞다(PPSSPP 는 EBOOT.BIN 선두가 `~PSP` 또는 ELF 인지 검사하고 평문 ELF 를
받아들인다). 그러나 **현재 파일로는 당장 불가**하며, 앞서 내가 낸 수치 3개가 틀렸다.

**정정 1 — EBOOT 슬롯**
EBOOT.BIN(LBA 32) 다음은 53,661섹터 빈 것이 아니라 **LBA 832 의 UPDATE/PARAM.SFO** 다.
연속 슬롯 = (832-32)*2048 = **1,638,400B, 여유 27,264B.**
일본판 평문 ELF 예상 1,610,888B(헤더 0x100 + elf_size 1,610,788)는 여유 27,512B 로 아슬하게 들어간다.

**정정 2 — 진짜 엔트리**
`~PSP` 헤더(우리 ISO EBOOT):
```
+0x28 elf_size   = 0x00189424 (1,610,788)
+0x2C psp_size   = 0x00189580 (1,611,136)
+0x30 load_addr  = 0x08804000
+0x34 entry_off  = 0x001612E0
```
실제 엔트리 = load + entry_off = **0x089652E0.** (검증 완료)
`build/EBOOT_dec.elf` 의 e_entry=0x896A904 는 ext(미국판) modinfo_off 0x166904 를 잘못 쓴 값.

**정정 3 — RAM 덤프 JIT 오염 (치명적)**
`jp/EBOOT_dec.bin` 은 꼬리 37,924B 부족만이 아니라, **코드 워드의 2.56%(10,081/393,216)가
JIT 마커 `0x68xxxxxx` 로 덮여 있다.** 첫 워드부터 `0x68001012` 다(§8 의 기존 경고 재확인).
-> 길이만 1,610,788B 로 늘려 ELF 로 감싸도 실행 불가.

**폐기 대상** `build/EBOOT_dec.elf`(미국판 암호문에 ELF 헤더만), `build/D2_KR_eboot.iso`(미국판 ISO).

**C안이 필요로 하는 것 (전부 미확보)**
1. JIT 오염 없는 복호본 — 인터프리터 모드 덤프, 또는 PPSSPP `DumpDecryptedEboots` 의 진짜 산출물.
   현재 `DumpDecryptedEboots=True` 인데도 DUMP 폴더/로그/메모리스틱에 ELF 산출물이 **없다.**
   "설정만 켜면 나온다"는 미검증 — 별도 확인 필요.
2. 엔트리 0x089652E0 로 세팅한 ELF 프로그램 헤더.
3. 올바른 PSP 모듈/임포트 정보 (부팅 시험으로만 확인 가능).

**결론**
- **A(멤버 203,000B 이하)**: 무패치, `pick_fw.py` 로 81%. 검증됨.
- **B(CWCheat 9워드)**: 동작 검증됨. 완전 재부팅부터 적용.
- **C(평문 ELF)**: 원리 OK, 현재 파일로는 불가. JIT 없는 복호 ELF 확보가 선결.
- 다음 단계로 C 를 살리려면 **인터프리터 모드(JIT off)로 깨끗한 복호 EBOOT 을 먼저 확보**해야 한다.
  이는 PPSSPP 설정 변경이므로 사용자 허락 필요.

### 23-15. ★★★ C안 성공 — PPSSPP 공식 복호 ELF를 ISO에 직접 탑재 (2026-08-23)

> 이 절이 §23-14의 결론을 대체한다. §23-14는 당시 보유한 잘못된/오염된 덤프에 대한
> 판정으로 보존한다.

현재 PPSSPP는 예전 `DumpDecryptedEboots`가 아니라 `DumpFileTypes` 비트 플래그를 쓴다.
`DumpFileTypes=1`로 원본 JP ISO를 한 번 부팅하자 다음 파일이 생성됐다.

```
C:\Users\OXP2\Documents\PPSSPP\PSP\SYSTEM\DUMP\ULJS00183_EBOOT.BIN
```

덤프 뒤 설정은 즉시 `DumpFileTypes=0`으로 복구했다. 이 파일은 RAM/JIT 덤프가 아니라
PPSSPP가 복호·압축 해제한 **정상 32-bit little-endian MIPS ET_EXEC ELF**다.

```
크기       1,611,136B
entry      0x08804000
PT_LOAD    file off 0x54, vaddr 0x08804000
filesz     0x188EB0
memsz      0x53B800
JIT 0x1A 마커 0개
```

`~PSP` 헤더의 `0x1612E0`은 entry offset이 아니라 PSP module-info offset이었다.
EBOOT 연속 슬롯도 정확히 `(832-32)*2048 = 1,638,400B`이며 이 ELF는 27,264B 여유로 들어간다.

재현 도구와 보존 파일:

```
tools/patch_eboot_buffer.py
build_jp/EBOOT_dec_clean.elf
build_jp/EBOOT_dec_talk36000.elf
build_jp/EBOOT_KR.BIN
```

`EBOOT_KR.BIN`에는 §23-12의 9워드가 모두 적용됐다. PPSSPP는 ISO의 `EBOOT.BIN`이
`~PSP` 또는 ELF이면 로드하므로, 평문 ELF 교체판은 CWCheat 없이 자기완결적으로 부팅한다.
사용자가 전량 전각 빌드에서 「새로 시작」과 전반 진행이 정상이라고 확인했다.

따라서 현재 배포 결론은 다음과 같다.

- **에뮬 전용: C안(평문 ELF 교체)이 최종안.** ISO 하나만 배포하면 된다.
- CWCheat은 더 이상 필수 아님.
- 실기 호환판이 별도로 필요할 때만 A/B안을 유지한다.

### 23-16. 숫자 빈칸 원인과 수정 — 전각 영숫자 62자 복원

사용자 실기 화면에서 대사는 정상이나 `수명 1년이나 2년쯤`의 `1`, `2`만 빈칸으로 나왔다.
원인은 전각 숫자 바이트가 아니라 폰트 매핑이었다.

```
원본 ０..９  SJIS 824F..8258  -> gid 132..141
한글 베이크                         gid 132..1756 재사용
기존 build_tables                   전각 영숫자 매핑을 0으로 제거
결과                               숫자 바이트는 있으나 빈 글리프
```

대사 고정 바이트 줄바꿈을 위해 빌드가 ASCII 숫자와 영문을 모두 전각으로 바꾸므로,
숫자 10개만이 아니라 원본 전각 `０..９Ａ..Ｚａ..ｚ` 62자를 함께 복원했다.
ASCII 글리프는 건드리지 않았다. 따라서 바이트코드에 섞인 ASCII 쓰레기를 숨기는 기존 장치는
그대로 유지된다.

수정 파일:

```
tools/krfont.py
  collect_fullwidth_alnum()
  move_glyphs_to()
  FULLWIDTH_ALNUM_BASE=1757

tools/bake_font.py
  원본 전각 영숫자 셀을 gid 1757..1818로 복사
  moved_codes.tsv 및 talk00.dat/fontB.fnt에 재매핑
```

정적 검증:

```
전각 영숫자 62자  gid 1757..1818, 중복 0, gid 0 매핑 0
62개 글리프 픽셀 합 최소 259, 빈 셀 0
０..９ 실제 픽셀 합: 797,354,755,769,663,792,858,491,874,810
```

### 23-17. 현재 최종 테스트 ISO

기존 `build_jp/D2_JP_KR.iso`가 실행 중이라 Windows 파일 잠금으로 덮어쓰지 못했다.
사용자의 실행 중인 PPSSPP를 종료하지 않고 별도 출력 경로를 지원하도록 다음을 추가했다.

```
D2_ISO_DST=build_jp/D2_JP_KR_alnum.iso
```

`tools/build_jp.py`, `tools/build_talk.py`, `tools/verify_iso.py`가 이 환경변수를 받는다.

최종 산출물:

```
build_jp/D2_JP_KR_alnum.iso
크기    854,360,064B
SHA256  5ec2e7c0d95b8511e84e24b2d79ec0c12785830a729764e3de717f82bcb1e0c7
```

ISO 되읽기 검증 결과:

```
START_JP.LZS   4,859,501B / 슬롯 99.4% / 무손상 OK
SCRIPTPACK     3,497,195B / 슬롯 99.86%
talk01         마지막 레코드와 파일 끝 차이 8B / 정합
EBOOT.BIN      정상 ELF 1,611,136B / 패치 9워드 전부 일치
전각 영숫자    62자 전부 gid 1757..1818
전체 검증      OK
```

다음 확인은 `D2_JP_KR_alnum.iso`를 완전 재부팅해 같은 장면에서 `１`, `２`가 보이는지
육안 확인하는 것뿐이다. 정적 데이터상 숫자 코드·매핑·픽셀은 모두 정상이다.


### 왜 앞선 결론이 엇갈렸나

`build_jp.py`는 START 안의 `sys2.txp`를 패치하고, `build_char.py`는 ISO 루트의
`/PSP_GAME/USRDIR/CHAR.DAT`를 별도로 패치한다. 숫자/영문 테스트용 별도 ISO는
`D2_ISO_DST=build_jp/D2_JP_KR_alnum.iso`로 만들었지만, 당시 `build_char.py`만 이 환경변수를
지원하지 않고 `build_jp/D2_JP_KR.iso`에 하드코딩되어 있었다.

두 ISO의 레코드 288(`ママ`) 비교가 결정적이었다.

| ISO | `sys2.txp[288]` | `CHAR.DAT[288]` | 화면 이름표 |
|---|---|---|---|
| `D2_JP_KR.iso` | `엄마` | `엄마` | `하나코` 등 한글 정상 |
| 패치 전 `D2_JP_KR_alnum.iso` | `엄마` | `ママ` | 일본어 잔존 |

`sys2`는 양쪽 모두 한글인데 화면 결과가 달랐고, 차이는 `CHAR.DAT`였다. 따라서
**대화창 화자 이름표의 실제 문자열 출처는 루트 `CHAR.DAT`**로 확정한다.

과거 RAM에서 `アデル`/`ママ` 원문이 다수 발견된 것은 talk 본문에 들어 있던 동일 문자열까지
함께 센 결과라 출처 판정 근거가 될 수 없었다. talk의 `0x3EF` 같은 순차 값도 화자 ID가 아니라
명령/이벤트 순번 계열이므로 그 가설은 폐기한다. 평문 ELF에는 이름 문자열이 0개이며,
MAPPACK/TXPPACK 검색 히트는 텍스처 픽셀의 우연한 바이트 일치였다.

### 수정

`tools/build_char.py`도 다른 빌드 도구와 동일하게 출력 ISO 환경변수를 받는다.

```python
dst = os.environ.get('D2_ISO_DST', 'build_jp/D2_JP_KR.iso')
```

별도 ISO에 재주입 후:

```
CHAR.DAT 적용 792회 / 미번역 0종
CHAR.DAT[0]   = 아델
CHAR.DAT[288] = 엄마
CHAR.DAT 내 ママ 잔존 = 0
```

현재 `build_jp/D2_JP_KR_alnum.iso`:

```
크기    854,360,064B
SHA256  5ec2e7c0d95b8511e84e24b2d79ec0c12785830a729764e3de717f82bcb1e0c7
```

### 필수 빌드 순서

```
python tools/bake_font.py
python tools/merge_tr.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso
python tools/build_char.py --iso   # 이름표를 위해 생략 금지
python tools/verify_iso.py
```

별도 출력이면 세 ISO 주입 단계 모두 같은 `D2_ISO_DST`를 설정해야 한다.

### 24-1. 최종 확인 결과

사용자가 `D2_JP_KR.iso`의 실제 대사 화면에서 화자 이름 **`하나코`**가 한글로 표시되는 것을
확인했다. 따라서 이름표 렌더러나 폰트 문제는 아니며, `CHAR.DAT` 주입 여부가 전부였다.

별도 테스트본 `D2_JP_KR_alnum.iso`에도 `build_char.py --iso`를 다시 실행해 동일하게 수정했다.
`tools/verify_iso.py`에는 다음 검사를 추가했다.

```
CHAR.DAT 구조: 8 + count*0x102
ISO CHAR.DAT == build_jp/CHAR.DAT 바이트 단위 일치
CHAR.DAT 내 ママ 잔존 0
```

최종 전체 검증 출력:

```
CHAR.DAT(이름표) lba 260448 / 396레코드 / 빌드 일치 True / ママ 잔존 0
START 무손상 검증 OK
SCRIPTPACK 오프셋 정합
전체 검증 OK
```

조사 중 만든 `tools/trace_nameplate.py`는 PPSSPP read watchpoint 추적용으로 남긴다. 이번 건은
두 ISO의 `CHAR.DAT` 차이와 인게임 교차 확인으로 원인이 확정되어 실제 watchpoint 실행은 필요 없었다.

**이름표 작업 상태: 완료.** 이후 별도 ISO를 만들 때 `build_char.py --iso`를 생략하거나
`D2_ISO_DST`를 다르게 주는 경우만 다시 일본어 이름표가 생긴다.

## 25. 나레이션(script09/16) + name.txp 아틀라스 (2026-08-23)

### 25-1. 나레이션 23건 번역 완료 (ISO 되읽기 검증)
script09(제논 사후 나레이션 21건) / script16(에필로그 카드 2건). talk 와 다른 포맷:
`32 <len> <SJIS> 00` 인라인, len = 문자열+NUL.

오프셋 테이블 재계산을 피하려고 **원문과 정확히 같은 바이트 길이**로 제자리 치환한다
(부족분은 전각 공백 8140 패딩 — 문장 끝이라 안 보임). 번역은 전 문자 2바이트(전각).
번역: `work/tr_script_narr.py`, 적용: `build_talk.py` 내 통합(멤버 크기 불변).
ISO 되읽기 23/23 적용·원문 잔존 0 확인.

### 25-2. TXPPACK 전수조사 + name.txp
TXPPACK.DAT = NISPACK 220 멤버. bg/wbg 배경 121 + bup 초상 89 + tcg 갤러리아트 8(8bpp)
+ bg235b + name.txp — **텍스트가 있는 것은 name.txp 뿐.**

`name.txp`: 256x256 4bpp 16색 그레이스케일+알파, **리니어(스위즐 없음 — unswizzle 하면 노이즈)**.
2열x16행, 슬롯 128x16 에 이름 이미지 22개(ママ/アデル/斧雪/魔王ゼノソ 등).
§24 확정대로 **대사창 이름표의 출처는 아니지만**(그쪽은 루트 CHAR.DAT), 이름이 그려진
실존 텍스처이므로 다른 화면에서 쓰일 가능성이 있다. 사용처 미확인 — 플레이 중 일본어
이름이 보이면 여기일 수 있다.

`tools/build_nameplate.py` 가 21개 슬롯을 한글 재렌더(？？？？ 만 원본 유지, 크기·팔레트 불변).
- 표기는 CHAR.DAT 번역과 통일 (프리니 부대 — tr_char1 의 '프리니 대'를 맞춰 수정함)
- `魔王ゼノソ`(ン→ソ 말장난) → `마왕 제뇬`(논→뇬)
- 렌더 함정: PIL stroke_width 는 12px 폰트 획을 잠식한다 — 본문 마스크 8방향 팽창으로
  외곽선을 만들고 색인(백15/외곽3)을 직접 찍는다.

### 25-3. 최종 빌드 순서 (v2 — 24절 순서에 nameplate 추가)
```
python tools/bake_font.py
python tools/merge_tr.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso        # 대사 + 나레이션
python tools/build_char.py --iso        # 이름표(대사창) — 생략 금지 (§24)
python tools/build_nameplate.py --iso   # name.txp 아틀라스
python tools/verify_iso.py
```

**남은 것: 릴리즈뿐** (xdelta + 빠른패처 + 카페 게시). PPSSPP 전용(평문 ELF EBOOT) 명시.

## 26. 범용 유닛 기본 이름 풀 — 루트 `NAME.DAT` (2026-08-23)

전투 준비 화면에서 발견. 두 증상이 같은 파일에서 나왔다.
- `ゼリク` `ユーニス` — 미번역 카타카나 이름 노출
- `の１２` — 개그 이름(한자 포함 82개)의 한자 글리프를 지웠으니 깨져 보임

### 포맷 (11,324B)
```
+0x000  u16 x4   그룹 카운트 206,206,206,206
+0x008  u16      그룹 카운트 86            -> 합 910
+0x00A  u16 x910 블롭 상대 오프셋
+0x726  NUL 종단 SJIS 이름 x910 (최장 16B)
```
검증: 910개 오프셋 중 909개가 SJIS 선두바이트에 정확히 착지(1개는 ASCII 시작 이름).
고유 906개. 한자 포함 82개(`あめ色たまねぎ` `うっとり部長` `ニコラス刑事` `桃色吐息` 등 개그).

### 적용 (`tools/build_names.py`)
오프셋 테이블을 새로 계산하므로 **블롭 길이는 자유**(슬롯 32,768B, LBA 248080..248096).
제약은 이름당 **16B 이하**뿐. 중복 이름은 오프셋을 공유해 블롭을 줄인다.
이름은 한글/전각만 쓴다(ASCII 혼입 금지 — 어서션으로 막음).

### 최종 빌드 순서 (v3)
```
python tools/bake_font.py
python tools/merge_tr.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso        # 대사 + 나레이션
python tools/build_char.py --iso        # 대사창 이름표 (§24) — 생략 금지
python tools/build_nameplate.py --iso   # name.txp 아틀라스 (§25)
python tools/build_names.py --iso       # 유닛 기본 이름 풀 (§26)
python tools/verify_iso.py
```

### 인게임 미확인 영역 (릴리즈 전 확인 권장)
빌드 검증은 통과했으나 화면으로 못 본 것 — 전부 고정 필드 폭이라 잘림 위험이 있다.
- HABIT.dat 기술·무기명 1,141종 (`허접 글러브` `누더기 천`은 확인됨)
- magic.dat 마법 설명 431필드
- dungeon.dat 이노센트 이름 125종
- script09/16 나레이션 23건 (스토리 후반)

## 27. 유닛 이름 풀 + 기호 크기 (2026-08-23)

### 27-1. ★ 이름 풀의 실제 출처는 `START_VM_JP.LZS` (루트 NAME.DAT 아님)
전투 준비 화면에 `ムサシ` `ハミルトン` `ジスカ` 가 그대로 나왔다.

루트 `NAME.DAT`(11,324B)를 규명·번역해 넣었는데 **화면이 안 바뀌었다.**
ISO 를 평문으로 전수 검색하니 히트가 1건뿐 — **압축돼 있었기 때문**이다.
`START_VM_JP.LZS`(1,399,871B) 를 해제하니 3,031,216B 이미지 안에 이름이 전부 있었다.

- 블롭 구조: **NUL 종단 가변길이 문자열 연속 배치** (고정 stride 아님)
- 오프셋 테이블이 딸려 있으므로 **각 이름의 바이트 길이를 유지**해야 한다
  -> `tools/build_vmnames.py` 가 제자리 치환(짧으면 NUL 패딩), 이미지 크기 불변
- 911개 치환. 재압축 1,399,575B / 슬롯 1,409,024B(99.3%), LZS 왕복·비중첩 제약 검증
- 번역 906건: `work/tr_names_1..3.py`. 서양식 이름은 관례 음차, 개그 이름은 뜻 현지화
  (`あめ色たまねぎ`→`엿빛양파`, `ニコラス刑事`→`니콜라스형사`, `中の人などいない`→`안에사람없음`)
- 원문 바이트를 넘는 12건은 축약(`どす恋`→`찐사랑`, `サバの味噌煮`→`고등어조림`, `鬼軍曹`→`악귀상`)

**루트 `NAME.DAT` 는 화면에 영향이 없다**(미사용으로 보인다). `build_names.py` 는 남겨두지만
실제 담당은 `build_vmnames.py` 다. 혼동 주의.

### 27-2. 전각 기호·영숫자 크기 맞춤
`？ ！` 와 숫자가 커 보인다는 지적. 원인은 **원본 게임의 전각 글리프를 그대로 쓴 것** —
일본어 폰트용이라 잉크 높이 12~13px, 우리 한글(돋움 12)은 11px.

`krfont.rebake_cell` / `rebake_symbols` 로 **같은 서체로 다시 굽는다.**
대상: 전각 기호 10자(`？！：，．（）－／～`) + 전각 영숫자 62자(원본 셀 복사 후 덮어씀).

★ 세로 위치는 `getbbox` 로 예측하면 폰트마다 어긋난다(`，`·`．`가 문장 중간에 떴고
`～`는 상단에 붙었다). **그린 뒤 잉크 박스를 재서 옮기는** 사후 정렬로 해결:
```
，．、。・  잉크 하단을 y=11 (베이스라인)
～－        잉크 중심을 y=6
그 외       한글 박스(y 1..11) 중앙
```

### 27-3. 최종 빌드 순서 (v4)
```
python tools/bake_font.py
python tools/merge_tr.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso        # 대사 + 나레이션
python tools/build_char.py --iso        # 대사창 이름표 (§24) — 생략 금지
python tools/build_nameplate.py --iso   # name.txp 아틀라스 (§25)
python tools/build_names.py --iso       # 루트 NAME.DAT (효과 없음, 보존용)
python tools/build_vmnames.py --iso     # ★ 유닛 이름 풀 실제 담당 (§27-1)
python tools/verify_iso.py
```

### 27-4. 인게임 확인 현황
확인됨: 대사·이름표·유닛 이름·클래스명·무기명(`장난감 활`,`누더기 천`)·스테이터스·숫자/기호 크기
**미확인(릴리즈 전 권장)**: 전투 중 **기술명**(HABIT 1,141종)·**마법 설명**(magic 431필드, 24자)
·**이노센트 이름**(dungeon 125종)·나레이션(스토리 후반).
바이트 여유는 전 2,286건 최소 1B 로 필드 안에 들어간다(여유 0 없음). 다만 **바이트가 맞아도
화면 폭에서 잘릴 수 있어** 눈으로 봐야 한다. 가장 빡빡한 예: `쿠노이치 블레이드`(17/20B),
`특화된 능력은 없지만 광범위 기술을 많이 익힌다.`(47/49B).

## 28. 한국어 타이틀 화면 (2026-08-23)

`TXPPACK.DAT :: wbg11.txp` = 타이틀 배경 **480x272, 8bpp 256색, 리니어**(스위즐 없음).
로고가 이미지에 통째로 박혀 있어 텍스트 패치로는 불가.

`tools/build_title.py` — `work/title_kr_imagegen.png` 를 중앙 크롭 후 480x272 로 축소,
MEDIANCUT + Floyd-Steinberg 로 256색 양자화(작은 화면 그라데이션 밴딩 억제),
원본 TXP 헤더 유지 -> 크기 131,600B 불변. 적용 문구 `마계전기 / 디스가이아 2 / PORTABLE`.

**★ 기존 TXPPACK 수정을 보존한다** — `build_jp/TXPPACK.DAT` 가 있으면 그것을 기반으로 삼아
`build_nameplate.py` 의 이름표(name.txp) 작업이 덮이지 않는다. 두 도구의 실행 순서는
`build_nameplate` -> `build_title` 이어야 안전하다(둘 다 TXPPACK 을 건드린다).

### 28-1. 최종 빌드 순서 (v5)
```
python tools/bake_font.py
python tools/merge_tr.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso        # 대사 + 나레이션
python tools/build_char.py --iso        # 대사창 이름표 (§24)
python tools/build_nameplate.py --iso   # name.txp 아틀라스 (§25)
python tools/build_title.py --iso       # ★ wbg11.txp 타이틀 (nameplate 다음에)
python tools/build_names.py --iso       # 루트 NAME.DAT (효과 없음, 보존용)
python tools/build_vmnames.py --iso     # 유닛 이름 풀 실제 담당 (§27-1)
python tools/verify_iso.py
```

### 28-2. 남은 미번역 (릴리즈 시 명시 대상)
- **오프닝 나레이션** (`それは宇宙に蔓延る邪悪な世界。` 등) — 텍스트 아님. ISO 평문·전 압축
  아카이브 해제 검색 모두 0건 -> 이미지 확정. 위치 미특정. **보류 결정(사용자)**.
  찾는 방법은 PPSSPP 텍스처 덤프가 가장 확실하다(설정 변경 필요).
- **지명 간판** (`ホルルト村` 등) — 사용자는 텍스트라고 판단. 그런데 패치본 전 파일에서
  원문 0건이고(script00·charhelp·InProgramTxtDB 모두 번역 완료, START_VM 계열도 0건)
  화면엔 일본어가 나온다. **RAM 추적 미완**. 다음 세션에서 디버거로 출처를 잡아야 한다.
  (유닛 이름 풀이 START_VM 안 사본이었던 것과 같은 유형일 가능성)

## 29. ISO 빠른패처 (2026-08-23)

OGMD 패처(`work_ogmd/iso_quickpatch`)와 같은 **제자리 구간 패치** 방식.
D2 는 원본·패치본 ISO 크기가 동일(854,360,064B)하므로 ISO 내부 파일 탐색이 불필요하고
**절대 오프셋 구간**만 담으면 된다 — OGMD 보다 단순하다.

### 구성
```
iso_quickpatch/build_range_pack.py   원본 vs 패치본 diff -> D2_ISO_ranges.bin
iso_quickpatch/D2_ISO_ranges.bin     구간 594개 / 페이로드 11,609,028B (ISO 의 1.36%)
iso_quickpatch/D2IsoQuickPatch.cs    WinForms GUI + CLI
iso_quickpatch/D2_ISO_QuickPatch.exe 11,633,664B (리소스 임베드)
```

### 리소스 포맷 (`D2PSPRNG1`)
```
magic "D2PSPRNG1" / u32 version / u16 titleLen + utf8 title
u64 isoSize / 32B srcHash / 32B dstHash / u32 rangeCount
rangeCount x { u64 offset, u32 length, length bytes }
```
MERGE_GAP=64 로 인접 변경을 합쳐 런타임 복사 횟수를 줄인다.

### 빌드
```powershell
python iso_quickpatch\build_range_pack.py

& 'C:\Windows\Microsoft.NET\Framework64\v4.0.30319\csc.exe' `
  /nologo /target:winexe /optimize+ `
  /out:'iso_quickpatch\D2_ISO_QuickPatch.exe' `
  /resource:'iso_quickpatch\D2_ISO_ranges.bin',D2_ISO_ranges.bin `
  'iso_quickpatch\D2IsoQuickPatch.cs'
```

### 동작
- **적용**: 원본 해시 확인 -> 원본 구간을 `<ISO>.d2bak` 로 백업 -> 구간 덮어쓰기 -> 결과 해시 검증
- **상태 확인**: 원본 / 패치됨 / 알 수 없음 판별
- **되돌리기**: `.d2bak`(포맷 `D2PSPBAK1`)으로 제자리 복원
- 읽기 전용 ISO, 크기 불일치, 이미 적용됨, 원본 아님 — 모두 진행 전에 막는다
- GUI 드래그 앤 드롭 지원. CLI: `D2_ISO_QuickPatch.exe <ISO> [--verify|--cli]`

### 실기 검증 (사본으로 왕복 테스트 완료)
```
원본        AFE3475C3754CF1FC4170798EE36213B4434D5C4C2E585B91E3917962A6DDC55
적용 후     EA2A3D0076C4B90CE026DE9766BEBC9E8DA83BA7743B6016FDE75F224B90C7D7  (최종 ISO 와 일치)
중복 적용   차단됨 ("이미 적용되어 있어 할 일이 없습니다")
백업 복원   AFE3475C… 원본 해시와 일치
```

**주의** 해시가 원본/패치본에 고정되어 있으므로, 빌드가 바뀌면
`build_range_pack.py` 를 다시 돌리고 EXE 를 재컴파일해야 한다.

## 30. 오프닝 나레이션 반영 완료 (2026-08-23)

### 30-1. 위치 (코덱스 규명)
오프닝 나레이션은 텍스트가 아니라 **512x512 4bpp 스위즐 아틀라스**다.
```
ANMPACK.DAT :: anm7101.dat
  resource  +0x40660 (크기 0x20720)
    +0x0320  16색 RGBA 팔레트
    +0x0720  512x512 4bpp 패킹 인덱스 (하위 니블 먼저, PSP 스위즐)
```
배경은 별개로 `TXPPACK/wbg06.txp`. 도구 `tools/build_opening_text.py`,
검증 `tools/verify_opening.py`, 아카이브 헬퍼 `tools/anm.py`.

### 30-2. ★ 외곽선이 핵심이다
1차 결과가 인게임에서 뭉개져 보였다. 아틀라스 자체는 선명했고 원인은 **외곽선 누락**이었다.
원본 팔레트는 두 구간으로 나뉘어 있다.
```
index 1~7   (0,0,8,a) 계열 = 검은 외곽선 알파 램프
index 8~15  회색~흰색      = 글자면
```
원본 일본어는 외곽선/글자면 픽셀 비율이 **1.45**. 보라색 배경 위 흰 글자만으로는
대비가 무너져 축소 시 번져 보인다. 수정본은 비율 **1.71** 로 원본과 동급이며 인게임 확인됨.

검증 방법: 아틀라스를 언스위즐해 인덱스 1~7 과 8~15 의 픽셀 수 비를 원본과 비교한다.

### 30-3. 메인 ISO 반영
코덱스 산출물(`build_jp/ANMPACK_opening.dat`)과 현재 ANMPACK 을 멤버 단위로 비교해
**`anm7101.dat` 하나만** 다른 것을 확인하고 그 멤버만 이식했다(ANMPACK 크기 불변 68,324,111B).
이렇게 하면 코덱스가 베이스로 쓴 ISO(`D2_JP_KR_title.iso`)와 내 최신 빌드가 갈라져 있어도
다른 작업(이름표·유닛 이름 등)이 되돌아가지 않는다.

### 30-4. 최종 산출물
```
build_jp/D2_JP_KR.iso   854,360,064B
sha256 4B74130AC970B7215D6B3609BB826374D08FA20648A03E1E0C4FBB3C40877518
```
패처 리소스·EXE 재생성 완료 (구간 684개 / 페이로드 11,680,191B / EXE 11,705,856B).
원본 사본 왕복 검증에서 위 해시와 일치 확인.

**주의** ISO 가 바뀌면 반드시 `build_range_pack.py` 재실행 + EXE 재컴파일 + 왕복 재검증.

## 31. 릴리스 (v20260823)

### 산출물
```
저장소   https://github.com/kimjh-eclipse/disgaea2-psp-korean
릴리스   https://github.com/kimjh-eclipse/disgaea2-psp-korean/releases/tag/v20260823
문서     https://kimjh-eclipse.github.io/disgaea2-psp-korean/

배포 ZIP  Disgaea2Portable_Korean_v20260823.zip   6,002,982B
          sha256 7401CDB109ED4D7F7F89B40AEA736EEC92141107B00F3475973CBAC95ED0DCAE
          내용: D2_ISO_QuickPatch.exe + 읽어주세요.txt

최종 ISO  854,360,064B
          sha256 4B74130AC970B7215D6B3609BB826374D08FA20648A03E1E0C4FBB3C40877518
원본 ISO  sha256 AFE3475C3754CF1FC4170798EE36213B4434D5C4C2E585B91E3917962A6DDC55
```

### 저장소에 올린 것 / 올리지 않은 것
올린 것 100개 파일 / 1.9MB — `tools/*.py`(37), `work/tr_*.py`(56), `work/GLOSSARY.md`,
`iso_quickpatch/*.py|*.cs`, `HANDOFF.md`, `README.md`, `docs/`(Pages 8페이지).

**올리지 않은 것** — ISO, 추출 아카이브, 폰트·텍스처, 복호 EBOOT, RAM 덤프,
패처 리소스(`D2_ISO_ranges.bin`)와 EXE, 인벤토리 TSV, `work/chunks/`.
전부 게임 데이터이거나 원문 텍스트 덤프다. 패처 배포물은 Release 첨부로만 배포한다.

{: 커밋 직전에 금지 확장자 검사를 넣어 확인했다. `.gitignore` 화이트리스트 방식이
의도대로 동작하지 않았으므로(디렉터리 재포함 규칙 문제) **올릴 경로를 명시적으로 `git add`**
하는 방식을 썼다. }

### GitHub Pages
`main` 브랜치 `/docs`, `remote_theme: just-the-docs/just-the-docs@v0.10.1`.
페이지 URL 은 `/install` `/renderer` 형태(뒤에 슬래시를 붙이면 404).

### Release 자산 파일명
**GitHub 은 자산 파일명에서 비ASCII 를 제거한다.** 한글 파일명으로 올리면
`디스가이아2_PORTABLE_한국어화_v20260823.zip` -> `2_PORTABLE_._v20260823.zip` 이 된다.
ASCII 파일명(`Disgaea2Portable_Korean_v20260823.zip`)으로 올려야 한다.

### 검증 경로
ZIP 압축 해제 -> 원본 사본에 적용 -> 해시가 최종 ISO 와 일치 -> PPSSPP 부팅 확인.
배포물 그 자체로 왕복 검증했다.

### 로컬 정리
11GB -> 2.8GB. 지운 것: 중간 시험 ISO 10개, US판 추출본(`ext/`), 루트 스크린샷·RAM 덤프,
`ffmpeg` 압축, 다른 게임 ISO.
**`jp/EBOOT_dec_clean.elf` 는 반드시 남긴다** — ISO 만으로 재생성 불가.

## 32. ★ 전각 공백 자간 15px → 7px (2026-08-23, 인게임 검증)

§6-B 에서 "JP판은 자간 EBOOT 패치 불필요"로 결론냈으나, **띄어쓰기만은 예외**였다.
전각 공백이 한글 한 글자와 똑같은 15px 을 먹어 단어 간격이 과하게 벌어졌다.

### 32-1. 자간이 어디서 오는가 (규명 완료)

그리기 루프는 `0x088A8748`(루프머리 `0x088A8BEC`). 문자마다 자간을 `0x1C8(sp)` 에
넣고 `0x088A9D20` 의 `addu s5, s5, v1` 로 x 좌표에 더한다. 후보는 넷뿐이다.

| 출처 | 값 | 만들어지는 곳 | 대상 |
|---|---:|---|---|
| `f22` | 15px | `0x088A8774` `lui v1,0x4170`(15.0f) → `mul.s f22,f0,f12` | **ASCII 공백 `0x20`** |
| `0x1BC(sp)` | 15px | `trunc.w.s f0,f22` → `0x088A8898` | 2바이트 글자 전부 |
| `0x1C0(sp)` | 7px | `lui v1,0x40E0`(7.0f) → `0x088A883C` | ASCII 영문자 |
| `0x1C4(sp)` | 14px | `lui a0,0x4160`(14.0f) → `0x088A8868` | `0x849F` = `─` (괘선 타일링) |

**`fontB.fnt` 을 고쳐도 공백 폭이 변하지 않던 이유**가 여기 있다. ASCII 공백은
`0x088A8BFC` 에서 따로 빠져 부동소수(`f22`)로 좌표만 밀고 루프로 되돌아간다 —
폰트 메트릭을 아예 보지 않는다.

셀은 14px, 자간은 15px 이다. 즉 모든 2바이트 글자에 1px 자간이 이미 들어 있고,
`─` 만 14px 로 1px 당겨 타일링이 이어지게 해 뒀다.

### 32-2. 왜 `f22`(=ASCII 공백) 가 아니라 `0x8140` 을 고쳤나

받은 가이드는 "`0x20` 의 자간을 `0x8140` 글리프 간격의 절반으로" 였는데,
**빌드된 대사의 공백은 전각 `0x8140` 이 195,075개, ASCII `0x20` 은 0개**다
(§23 의 줄자르기 제약 때문에 전각화한 것). `f22` 를 고쳐도 화면에 변화가 없다.

그래서 손댈 곳은 **2바이트 `0x8140` 의 자간**이다. 이쪽이 오히려 낫다 —
2바이트 정렬이 유지되므로 §23 의 줄자르기 문제가 아예 발생하지 않는다.

### 32-3. 구현 — 영문자 판정 압축으로 공간 확보

자간 분기는 `0x088A8C7C` 의 `beql a0, zero` 하나다(`a0 != 0` → 7px).
`a0` 은 원래 "ASCII 영문자인가" 판정인데 원본이 11워드를 쓴다.
이것을 5워드로 압축하고 남은 6워드에 `0x8140` 검사를 넣었다.

```
0x088A8C1C  addiu v0, s2, -0x41      ; s2 = 선행바이트
0x088A8C20  sltiu v1, v0, 26         ; A-Z
0x088A8C24  addiu v0, s2, -0x61
0x088A8C28  sltiu a0, v0, 26         ; a-z
0x088A8C2C  or    a0, a0, v1         ; 영문자
0x088A8C30  lbu   v1, 0(fp)          ; 후행바이트 (fp 는 이미 전진해 있다)
0x088A8C34  sll   v0, s2, 8
0x088A8C38  or    v0, v0, v1         ; 문자코드 16비트
0x088A8C3C  xori  v0, v0, 0x8140
0x088A8C40  sltiu v0, v0, 1          ; == 0x8140
0x088A8C44  or    a0, a0, v0         ; ★ a0 = 영문자 || 전각공백
```

`tools/patch_eboot_buffer.py` 의 `ADVANCE_PATCHES` (버퍼 패치 9워드와 같은 표).

**왜 이 방식인가** — 대안 두 개를 버렸다.

| 대안 | 왜 버렸나 |
|---|---|
| `0x849F` 경로(14px) 하이재킹 — 3워드로 끝난다 | `───────` 괘선이 15px 로 밀려 1px 씩 벌어져 점선이 된다 (script00 `0x240`, `0x3FD`) |
| 트램폴린으로 자유 공간에 새 코드 | **`.text` 에 48B 이상 0 패딩이 0개**. PT_LOAD `filesz` 확장은 bss 선두를 침범해 위험 |

압축 방식은 새 공간·새 상수가 필요 없고 `0x849F` 의 14px 도 보존된다.
자간이 배율(`f12`)을 타는 성질도 `0x1C0(sp)` 를 그대로 쓰므로 유지된다.

### 32-4. 레지스터 안전성 (확인 완료)

- `v0`/`v1` — 바로 뒤 `0x8C4C`/`0x8C5C` 에서 재정의된다. 죽어 있다.
- `a0` — `0x088A9CA0` `andi a0, s2, 0xffff` 에서 덮어써진다. 죽어 있다.
- `s1` — 건드리지 않는다(`0x8C48` 이 같은 바이트를 다시 읽는다, 무해).
- `fp` — 루프는 **항상 2바이트를 소비**하고(`0x8BF4`, `0x8C6C`) 1바이트 문자는
  `0x088A9C48/9C64/9C7C/9C94` 의 `addiu fp, fp, -1` 로 되돌린다. 우리 코드는 fp 를
  읽기만 한다.
- 문자열 끝의 1바이트 문자에서 `lbu v1, 0(fp)` 는 NUL 종단자를 읽는다 — 범위 내.

### 32-5. 검증

1. 20워드 전부 원본 기댓값 일치 확인 후 교체 (`patch_eboot_buffer.py` 가 강제)
2. 패치본 재디스어셈블 — **첫 시도에 `rs` 필드를 손계산으로 틀렸고**
   (`0x2442FFBF` = `addiu v0,v0,..`, 정답은 `0x2642FFBF` = `addiu v0,s2,..`)
   디스어셈블에서 잡았다. **인코딩은 손으로 계산하지 말고 어셈블 함수를 쓸 것.**
3. **선행·후행 65,536 조합 전수 시뮬레이션 → 달라지는 것은 정확히 1개**
   (`0x8140`: 15px → 7px). 영문자 판정은 완전 보존.
4. ISO 제자리 교체 (LBA 32, 크기 1,611,136B 동일) — §23 에서 뻗었던
   레이아웃 이동 위험이 없다.
5. 인게임 확인 — 정상 부팅, 띄어쓰기 폭 자연스러움, 글자 정렬 이상 없음.

### 32-6. 남은 여지

문장부호도 같은 방법으로 좁힐 수 있다. `xori`+`sltiu 1`(정확일치)을
`addiu`+`sltiu N`(범위)로 바꾸면 **워드 수가 똑같다**. `0x8143`(`，`)
`0x8144`(`．`) 를 7px 로 만들면 한국어 조판에 더 맞는다. 다만 범위로 잡으면
`0x8141`(`、`) `0x8142`(`。`) 도 함께 들어가므로 미번역 일본어 문장의
구두점 폭도 바뀐다. 미적용.

## 33. ★★ 치명적 결함: 고정 레코드 DB 바이너리 파괴 (2026-08-24)

v20260823 배포본에서 **무기를 장착해도 「공격」이 동작하지 않는다**는 제보를 받았다.
원인은 번역 파이프라인이었다.

### 33-1. 원인

`recdat.put()` 이 문자열을 쓸 때 **필드 폭 전체를 NUL 로 채웠다.**

```python
out[b:b + w] = new + b' ' * (w - len(new))     # 옛 코드
```

그런데 `SPEC` 에 선언한 폭이 실제 문자열 영역보다 넓었다. 문자열 뒤는 바이너리
(무기 종류·사거리·공격력)였고, 그것이 전부 0 이 됐다.

| 파일 | 선언 폭 | 실제 폭 | 파괴된 레코드 | 소실 |
|---|---:|---:|---:|---:|
| `HABIT.dat` +0x15 (아이템·무기 DB) | 87 | **57** | 344 / 608 | 6,286B |
| `dungeon.dat` +0x00 | 64 | **32** | 119 / 125 | 2,402B |
| `mitem.dat` +0x20 | 72 | **64** | 77 / 86 | 254B |

`HABIT.dat` rec0 의 꼬리 `0101 0000 0f00 0100 1e00 …` 이 무기 파라미터다.

`sys2.txp`(캐릭터 DB, 0xF6/0x17)와 루트 `CHAR.DAT`(0x102/0x17)도 같은 방식으로
쓰지만 그쪽은 문자열 뒤가 원래 0 이라 무사했다 — 검사로 확인했다.

### 33-2. 실제 폭을 구하는 방법

원본에서 필드마다 다음 두 값을 재고, 그 사이를 폭으로 잡는다.

```
maxStr  = max(문자열 길이 + 1)                      # 최소한 이만큼은 문자열이다
dataAt  = min(NUL 뒤 첫 0 아닌 바이트의 위치)        # 여기부터는 바이너리다
안전폭  = dataAt   (maxStr <= dataAt 이어야 정합)
```

`tools/check_recdat.py` 가 이 계산을 하고 선언 폭이 `dataAt` 를 넘으면 실패시킨다.

### 33-3. 조치 (4중)

1. **폭 정정** — 87→57, 64→32, 72→64
2. **`put()` 비파괴화** — 문자열 + 종단 NUL 만 쓰고 **뒤는 원본 그대로 남긴다.**
   게임은 NUL 까지만 읽으므로 잉여 바이트는 무해하다. 폭을 또 틀려도 데이터가 죽지 않는다.
3. **`recdat.capacity()`** — 선언 폭을 믿지 않고 **레코드마다** 원본에서 가용 바이트를
   계산한다. `build_jp.py` 의 예산 검사도 이걸 쓴다(전에는 `w-1`).
4. **`tools/check_recdat.py`** — "문자열 영역 **밖**의 바이트가 한 개도 안 변했는가".
   `verify_iso.py` 에 편입했다.

폭이 줄어 초과된 아이템 설명 4건은 직접 축약했다(`work/tr_rec05.py`, `tr_rec06.py`).

### 33-4. ★ 왜 못 잡았나 — 검증의 구조적 결함

기존 검증은 **전부 문자열에 대한 것**이었다.

| 통과한 검사 | 왜 못 잡았나 |
|---|---|
| 번역률 100% | 문자열은 정상이었다 |
| 한자 잔존 0 | 문자열은 정상이었다 |
| 한글 표시 확인 | 문자열은 정상이었다 |
| 인게임 부팅·대사 확인 | 파괴된 것이 **화면에 나타나지 않는다** |
| START 무손상 검증 | 멤버 **단위**로만 비교했다. 고쳐야 하는 파일이므로 "변경됨"이 정상 |

`mitem.dat` 의 "한글 66/172" 라는 낮은 비율이 보였는데도 "BGM 제목이 ASCII 라
정상" 이라고 넘겼다. **비율 이상을 설명으로 덮은 것이 놓친 지점이다.**

**교훈**: 바이너리와 문자열이 같은 레코드에 섞여 있으면,
"문자열이 맞게 들어갔나" 와 별개로 **"문자열 영역 밖이 안 변했나"** 를 반드시 본다.
전자는 보이고 후자는 안 보인다. 안 보이는 쪽이 게임을 망가뜨린다.

### 33-5. 부수 영향

- 폭 전체를 NUL 로 채우지 않으니 잉여 바이트가 남아 압축률이 떨어졌다.
  `START_JP.LZS` 4,857,655B → **4,873,436B / 슬롯 4,886,528B (99.7%)**.
  여유가 13KB 뿐이다. START 쪽 텍스트를 늘릴 공간이 거의 없다.

### 33-6. 남은 정리거리

아이템 설명 **104건에 `：` 뒤 불필요한 공백**이 들어가 있다(301건은 없음).
일본어 원문에도 없다. 이번 수정과 섞지 않기 위해 손대지 않았다.

## 34. ★★ 지명 간판 이미지 한글화 확정 (2026-08-26)

거점 진입 시 표시되는 `ホルルト村` 등의 지명은 텍스트가 아니라
`ANMPACK.DAT/anm7151.dat` 안에 구워진 이미지다. 최종적으로 12개 지명을 모두
한글화했고, 인게임에서 `홀루트 마을`이 한 줄로 정상 표시되는 것을 확인했다.

### 34-1. 최종 규격 — 두 시트를 절대 합치지 말 것

`anm7151.dat`의 관련 영역은 다음과 같다.

| 오프셋 | 크기 | 내용 |
|---:|---:|---|
| `0x10C0` | 1024B | RGBA 팔레트. 간판은 첫 16색 알파 램프 사용 |
| `0x14C0` | **65,536B** | **간판 시트: 256×512 CLUT4**, PSP swizzled, row stride 128B |
| `0x114C0` | **65,536B** | 캐릭터 스프라이트 시트. 간판 작업에서 변경 금지 |

간판 안의 지명 배치는 다음과 같다.

```
폭 256px / 행 높이 32px
첫 행 y=64
행 r의 범위 = y 64 + r*32 .. 95 + r*32   (r=0..11)
```

PPSSPP 텍스처 덤프가 `0x098A6600`, **256×512 RGBA**로 나왔고, 원본 131,072B를
65,536B씩 나누어 각각 `rowbytes=128, height=512`로 언스위즐한 결과도 확정적이다.

- 첫 절반만 디코딩: 간판 프레임과 일본어 지명 12개
- 둘째 절반만 디코딩: 캐릭터 애니메이션 시트

확인용 산출물:

- `work/sign_sheet0_orig_x2.png` — 원본 간판 시트
- `work/sign_sheet1_orig_x2.png` — 뒤쪽 캐릭터 시트
- `work/sign_runtime_a.png`, `work/sign_runtime_b.png` — PPSSPP 실측 덤프
- `work/sign_kr_x4.png` — 현재 한글 간판 미리보기

### 34-2. 반복 실패의 원인과 배제된 해석

131,072B라는 전체 크기만 보고 한 장으로 처리한 것이 모든 실패의 원인이었다.

| 잘못된 해석 | 화면 결과 | 실제 원인 |
|---|---|---|
| 256×512 CLUT8 한 장 | 글자 상하 분리·점선·겹침 | CLUT4의 두 니블을 한 색인으로 합침 |
| 512×512 CLUT4 한 장 | 같은 지명이 세 줄처럼 반복 | 서로 독립인 간판/캐릭터 시트를 한 폭으로 언스위즐 |
| `0xFF`, `0xF0`, `0x0F`, `0xF1` 단일 색인 시험 | 불투명 중복 또는 반투명 점선 | 비트/니블을 합성 레이어로 오인한 임시 처방 |
| 좌·우 128px 열 중 하나를 비움 | 문제 미해결 | 애초에 8bpp/128px 열 구조가 아님 |

특히 `0xF1`을 8bpp 픽셀로 썼을 때 실제 CLUT4에서는 `1, F, 1, F...` 두 픽셀로
풀리므로 가로 점선이 된다. 색인을 바꿔가며 시험할 문제가 아니었다.

ANM 헤더의 시트 기술자는 외부/센티널 값(`bpp=4`, `off=0`, `width=height=0`)이라
기존 `tools/anm.py`만으로 크기를 얻을 수 없었다. 최종 크기는 PPSSPP의
`SaveNewTextures=True` 실측으로 확정했으며, 확인 후 설정은 다시 `False`로 복구했다.

### 34-3. 최종 구현

`tools/build_signatlas.py`가 다음 순서로 처리한다.

1. 기준 멤버는 항상 `jp/anm7151.dat`에서 읽는다(재실행 누적 방지).
2. `0x14C0`부터 정확히 65,536B만 꺼낸다.
3. 바이트 stride 128, 높이 512로 PSP 언스위즐한다.
4. 각 바이트를 `low nibble → 왼쪽`, `high nibble → 오른쪽` 순으로 풀어
   256×512 색인 이미지로 만든다.
5. 12개 지명 행만 지우고 굴림 22px로 가운데 정렬해 다시 그린다.
6. 글꼴 그레이스케일을 원본 첫 16색의 알파값과 가장 가까운 색인으로 매핑한다.
   팔레트는 변경하지 않는다.
7. CLUT4로 다시 패킹하고 stride 128로 스위즐한다.
8. 첫 65,536B만 교체하며 `0x114C0` 이후가 원본과 같은지 단언한다.

최종 번역 12개:

1. 홀루트 마을
2. 다로스 대하
3. 마의 대공동
4. 콜로세움
5. 콜로세움 집회장
6. 콜로세움 지하제단
7. 알케 시티
8. 신라의 탑
9. 제논 성으로 가는 길
10. 제논성 성문
11. 제논 성
12. 수수께끼의 성

### 34-4. 검증 및 현재 상태

- 12개 행 모두 비어 있지 않으며 bbox가 256×32 행 안에 들어옴
- 가장 긴 행도 폭 제한 256px 이내
- `ANMPACK.DAT` 크기 **68,324,111B 불변**
- ISO의 `ANMPACK.DAT` LBA **282896**, 제자리 교체
- `tools/verify_iso.py build_jp/D2_JP_KR.iso` 전체 검증 OK
- ANMPACK 전체에서 같은 간판 팔레트 첫 16색을 검색한 결과
  **`anm7151.dat` 하나만 일치**
- 인게임 확인: `홀루트 마을` 한 줄 정상 표시(겹침·점선 없음)

현재 최종 ISO는 `build_jp/D2_JP_KR.iso`다. 이후 간판 글꼴이나 번역을 바꿀 때는
`NAMES`/폰트 설정만 수정하고 반드시 `tools/build_signatlas.py --iso`로 재생성한다.

## 35. ★★ ULJS00183 추가 패키지 구조 규명·전량 한글화 (2026-08-29)

`../ULJS00183.zip`의 일본어판 DLC를 분석해 본편과 별도의 느슨한 파일 패치로
완성했다. ISO 안에 넣는 데이터가 아니므로 **본편 xdelta와 DLC xdelta는 별도**다.

### 35-1. 설치 구조

압축을 풀면 다음 19개 파일이 한 폴더에 바로 놓인다. 중첩 폴더를 만들지 않는다.

```
PSP/GAME/ULJS00183/
  PARAM.PBP
  DL_JP_00.EDAT ... DL_JP_17.EDAT
```

현재 PPSSPP에는 다음 위치로 설치했다.

`C:\Users\OXP2\Documents\PPSSPP\PSP\GAME\ULJS00183`

`PARAM.PBP`는 `CATEGORY=MG`, `DISC_ID=ULJS00183`인 메타데이터 셸이며 실행 파일은
없다. 게임 EBOOT가 `DL_JP_%02d.EDAT`를 직접 찾는다.

### 35-2. 18개 EDAT은 실제로 5개 누적 세대

각 EDAT은 평문 `NISPACK`이고 멤버는 항상 세 개다.

- `sound_02_jp.dat`
- `cutinPack.dat`
- `start_jp.lzs`

18개 파일은 아래처럼 정확히 같은 5개 스냅샷으로 묶인다.

| 대표 | 같은 파일명 | 의미 |
|---:|---|---|
| 00 | 00–04 | 1세대 |
| 05 | 05–08 | 2세대 누적 |
| 09 | 09–12 | 3세대 누적 |
| 13 | 13–16 | 4세대 누적 |
| 17 | 17 | 최종 누적 |

파일명이 로더 키일 수 있으므로 5개만 설치하면 안 된다. **18개 이름은 모두 유지**하되,
배포 패치는 대표 5개에만 xdelta를 적용하고 같은 세대 이름으로 복제한다.

### 35-3. 번역 범위와 톤

본편 번역표로 이미 덮이는 문자열을 제외하고 DLC 신규 고유 문자열 **1,604건**을
직접 번역했다. NLLB 시험 파일은 품질이 낮아 참고용으로만 남겼고 빌드에는 쓰지 않는다.

| 파일 | 신규 고유 |
|---|---:|
| `InProgramTxtDB.dat` | 1,548 |
| `char.dat` | 45 |
| `charhelp.dat` | 10 |
| `sys2.txp` | 1 |

번역표:

- `work/tr_dlc_dialogue_01.py` ... `tr_dlc_dialogue_11.py`
- `work/tr_dlc_fixed.py`

말투는 본편 용어집을 그대로 따른다. 토로는 `~냐`, 쿠로는 `~먀`, 로자리는
`짐/~느니라`, 하나코는 아델을 `오빠`로 부른다. 알마스·사파이어·다크 에클레르·
플레네르 등 기존 표기도 그대로 사용했다. `￥`, `π`, `η`, `Э` 제어문자는 보존했다.

### 35-4. 빌드 방식

`tools/build_dlc.py`가 각 대표 EDAT의 `start_jp.lzs`를 풀어 다음만 수정한다.

1. DLC 누적 `InProgramTxtDB.dat` — 본편 2,911건 + DLC 1,548건 적용, 대사 전각화
2. `sys2.txp`, `char.dat`, `charhelp.dat` 등 고정 DB — 레코드별 실제 용량 검사
3. 본편 최신 `script00.dat`와 폰트 4종 — **같은 한글 코드표 강제**
4. 나머지 DLC 전용 `waku.txp`, `anm0000.dat`, 음성, 컷인은 원본 그대로 보존

중요: DLC에 새 음절이 생기면 `tools/bake_font.py`가 코드표를 다시 고르므로,
반드시 다음 순서로 빌드한다.

```
python tools/bake_font.py
python tools/build_jp.py --iso
python tools/build_talk.py --iso
python tools/build_dlc.py
python tools/verify_dlc.py
```

본편을 다시 인코딩하지 않고 DLC만 만들면 START의 `script00.dat`뿐 아니라 ISO 별도
아카이브인 **`SCRIPTPACK.DAT` 전체 대사도 폰트 코드표와 어긋난다.** 실제 첫 DLC
빌드에서 START만 재인코딩해 기존 대사가 전부 다른 한글로 표시되는 회귀가 발생했고,
`tools/build_talk.py --iso`로 9,822건/63,120회 대사를 새 코드표로 다시 만들어 복구했다.
`tools/code_sync.py`의 지문 표식을 추가했으므로 이제 font/START/SCRIPTPACK 셋 중
하나라도 현재 코드표와 다르면 `build_dlc.py`가 시작 전에 중단한다.

이번 최종본은 위 순서로 동기화했으며 본편 `verify_iso.py`도 전체 통과했다.

### 35-5. 최종 검증

`tools/verify_dlc.py` 결과:

- 5개 세대 모두 NISPACK/START 왕복 성공
- `sound_02_jp.dat`, `cutinPack.dat` 원본과 동일
- START의 비대상 7개 멤버 원본과 동일
- 고정 DB 크기 불변
- 같은 세대 별칭 파일 SHA-256 동일
- DLC 신규 일본어 원문 잔존 **0건**
- `PARAM.PBP` 원본과 동일

산출물:

- 완성 DLC: `build_dlc/ULJS00183_KR/` (18 EDAT + PARAM.PBP)
- 해시: `build_dlc/manifest.json`
- 배포 ZIP: `build_dlc/D2_DLC_KR_patch.zip`
- 배포 폴더: `build_dlc/D2_DLC_KR_patch/`

배포 폴더에는 `group_00/05/09/13/17.xdelta`, `xdelta.exe`,
`DLC_패치_적용.bat`, 설명서가 있다. 실제 원본 DLC에서 배치를 시험해 19개 결과가
직접 빌드본과 SHA-256 **전부 일치**하는 것을 확인했다.

### 35-6. 현재 상태

- 최신 본편 ISO: `build_jp/D2_JP_KR.iso`
- DLC: PPSSPP 경로에 설치 완료
- PPSSPP 실행 완료
- 정적/구조 검증은 완료. **인게임 DLC 의제 진입과 대사 육안 확인은 사용자 확인 대기**

## 36. v20260830 ISO + DLC 통합 릴리즈 패처

`iso_quickpatch/D2IsoQuickPatch.cs`를 본편 ISO와 느슨한 DLC를 한 번에 처리하도록
확장했다. GUI 입력은 두 개다.

1. 일본판 원본 ISO
2. 선택 사항인 `PSP/GAME/ULJS00183` DLC 폴더

`한국어화 적용`을 누르면 ISO 구간 패치를 먼저 적용하고, DLC 경로가 지정됐으면
`DL_JP_00.EDAT`~`DL_JP_17.EDAT`을 **각 파일별로** 확인한다. 없는 번호는 건너뛰고,
원본 SHA-256과 맞는 파일만 해당 누적 세대의 xdelta를 적용한다. 이미 한국어판
해시인 파일은 재적용하지 않으며, 어느 쪽 해시도 아닌 파일은 안전을 위해 건드리지
않는다. 따라서 DLC를 일부만 설치한 사용자도 그대로 쓸 수 있다.

배포 sidecar:

```
D2_Korean_QuickPatch.exe
D2_ISO_ranges.bin
xdelta.exe
D2_DLC_group_00.xdelta
D2_DLC_group_05.xdelta
D2_DLC_group_09.xdelta
D2_DLC_group_13.xdelta
D2_DLC_group_17.xdelta
```

ISO는 기존처럼 변경 구간을 `<ISO>.d2bak`에 저장한다. DLC는 적용되는 각 EDAT의
원본을 `<EDAT>.d2bak`으로 보존한다. 통합 `백업으로 되돌리기`는 양쪽 백업을 함께
복원한다. 명령행은 다음 형식도 지원한다.

```
D2_Korean_QuickPatch.exe --cli "원본.iso" --dlc ".../PSP/GAME/ULJS00183"
D2_Korean_QuickPatch.exe --cli "패치본.iso" --dlc ".../ULJS00183" --verify
D2_Korean_QuickPatch.exe --cli "패치본.iso" --dlc ".../ULJS00183" --restore
```

실제 원본 ISO와 DLC 대표 파일 5개만 둔 부분 설치 폴더로 통합 적용을 시험했다.
ISO 결과 SHA-256은 `A1A5D85EA6EACF6E7F88B8EF8B9F88C1CCD2C1B15D67BACCEDE79E01E1DA40F7`,
각 DLC 결과는 `build_dlc/manifest.json`과 일치했다. 존재한 5개만 패치되고 누락된
13개는 생성되지 않았으며, ISO와 5개 EDAT의 `.d2bak` 백업도 모두 생성됐다.

이후 18개 EDAT 전체로 확대해 적용→복원→재적용 왕복도 시험했다. 복원 후 ISO는
원본 SHA-256, DLC 18개는 각 원본 SHA-256과 모두 일치했고, 재적용 후에는 다시
완성 ISO와 DLC manifest 해시에 전부 일치했다. 최종 배포 폴더는
`release_v20260830/`, ZIP은 `D2_Korean_v20260830.zip`이다.
